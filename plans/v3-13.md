# V3-13 执行计划 · demo 项目无破坏重跑工具(demo_reset.py)

> 本计划是 V3-13 在 Plan-Execute-Review 协议下的 Phase 1 产出。审核通过后进 Phase 2 自主 commit。
>
> V3-13 是 1-2h 工具债清理活——V3-3 baseline 衍生:跑 parse_tender 取数据时用"--force + git restore"组合,利用 ensure_reviewed 不查 hash 的实现细节,**方法论不干净**。V3-13 提供 `scripts/demo_reset.py` 标准化路径。
>
> Plan 阶段已实测 ensure_reviewed 行为 + git tracked 文件清单 + demo output/ 当前状态(对齐 feedback_phase0_paste_command_output 教训:**实测命令输出贴 plan,不只写结论**)。

---

## 0. Phase 0 关键发现(实测命令输出贴 plan)

### 0.1 ensure_reviewed 行为确认

`scripts/brief_schema.py` L30-62 实测:

```python
def ensure_reviewed(output_dir) -> bool:
    output_dir = Path(output_dir)
    tender_brief_path = output_dir / 'tender_brief.json'
    marker_path = output_dir / REVIEW_MARKER_FILENAME  # 'tender_brief.reviewed'

    if not tender_brief_path.exists():
        raise RuntimeError(f"未找到 tender_brief.json: {tender_brief_path}\n请先执行阶段 1...")
    if not marker_path.exists():
        raise RuntimeError(f"未找到 review 标记文件: {marker_path}\n用户 review 未完成。...")
    return True
```

**确认**:
- 只检查 2 件事:`tender_brief.json` 存在 + `tender_brief.reviewed` 存在
- **不查任何 hash**——这就是 V3-3 衍生 V3-13 的根因 (commit `a160653`):"baseline 跑 parse_tender 用 --force + git restore 组合,利用 ensure_reviewed 不查 hash 的实现细节,方法论不干净。"

### 0.2 demo output/ git tracked 文件清单(命令输出 paste)

```bash
$ git ls-files projects/demo_cadre_training/output/ | wc -l
43
```

完整 43 个文件清单(命令输出原样,缩短显示):

```
projects/demo_cadre_training/output/_timing.json
projects/demo_cadre_training/output/_timing_report.md
projects/demo_cadre_training/output/b_mode/<其他资格证明文件>/.pending_marker
projects/demo_cadre_training/output/b_mode/<其他资格证明文件>/assembled.docx
projects/demo_cadre_training/output/b_mode/<其他资格证明文件>/intermediate.json
projects/demo_cadre_training/output/b_mode/<其他资格证明文件>/manifest.yaml
projects/demo_cadre_training/output/b_mode/<商务部分>/.pending_marker
projects/demo_cadre_training/output/b_mode/<商务部分>/assembled.docx
projects/demo_cadre_training/output/b_mode/<商务部分>/intermediate.json
projects/demo_cadre_training/output/b_mode/<商务部分>/manifest.yaml
projects/demo_cadre_training/output/c_mode/<6 个子目录>/{filled,template}.docx
projects/demo_cadre_training/output/c_mode/<6 个子目录>/{intermediate.json,variables.yaml}
projects/demo_cadre_training/output/chapters/part_09/chapter_{01..05}_*.md
projects/demo_cadre_training/output/compliance_metrics.json
projects/demo_cadre_training/output/compliance_report.md
projects/demo_cadre_training/output/outline.md
projects/demo_cadre_training/output/scoring_matrix.csv
projects/demo_cadre_training/output/tender_brief.json
projects/demo_cadre_training/output/tender_brief.md
projects/demo_cadre_training/output/tender_raw.txt
projects/demo_cadre_training/output/tender_response.docx
```

**关键观察**:**`tender_brief.reviewed` 不在 git tracked 列表中**。这证明 .reviewed 标记不入 git(红线 1:用户手动建),所以 demo_reset 只 restore 这 43 个 tracked 文件就够,**不动 .reviewed 标记**。

### 0.3 demo output/ 当前 git 状态

```bash
$ git status --short projects/demo_cadre_training/output/
?? "projects/demo_cadre_training/output/b_mode/<其他资格证明文件>/raw.txt"
```

唯一 untracked 文件 `raw.txt` 是历史遗留(无关 V3-13)。其他 43 个 tracked 文件无 modified——demo HEAD 状态干净, 是 demo_reset 的 baseline。

### 0.4 模拟 --force 跑 parse_tender 后哪些文件被改

静态分析 `scripts/parse_tender.py` _main_body(L679+):
- 写 `output/tender_brief.json` (L723)
- 写 `output/tender_raw.txt` (L727)
- 写 `output/tender_brief.md` (L734)

`_timing_hook.stage_timer` 副作用更新:
- `output/_timing.json` (timer 上下文管理器结束时)
- `output/_timing_report.md` (同上)

下游 build_scoring_matrix / generate_outline / b_mode_run / c_mode_run / compliance_check 各自写各自的产物;parse_tender **不动它们**。但用户重跑 parse_tender 后,下游产物可能与新的 brief.json 不一致(如 brief.json 重置了 extracted 字段,但 outline.md / scoring_matrix.csv 还是旧的)。

→ demo_reset 必须 restore **全部 43 个 tracked 文件**(不只 brief.json),保证 baseline 整体一致。

---

## 1. 范围与完成判定

### 1.1 范围

**做**:

- 新增 `scripts/demo_reset.py`,1 文件
- CLI 接口:`--dry-run` (默认显示要做的事不执行) / `--yes` (确认执行) / `--help`
- 行为:`git restore projects/demo_cadre_training/output/` (恢复全部 43 个 tracked 文件) + 提示用户"现可跑 parse_tender --force"
- **不动** untracked 文件 (raw.txt 等)
- **不动** `.reviewed` 标记 (红线 1 兼容)
- **不调** `git clean` (避免删 untracked)
- **不调** `git checkout` (避免 detach HEAD 风险)
- 误用防护:hardcode demo 项目路径,不加 `--project` flag (V3 范围内仅服务 demo_cadre_training)
- 文档化:在 `docs/FAQ.md` 加 1 条 Q "demo 项目重跑 parse_tender 怎么干净开始"

**不做**:

- 不加 `--project` flag (hardcode demo_cadre_training,V3 范围)
- 不动 SKILL.md (留 V3-10 阶段口径统一时一并处理)
- 不写 git commit / git push (脚本只读 git ls-files + 写 git restore)
- 不重跑 parse_tender (脚本只 reset, 跑 parse_tender 由用户)
- 不删 `_timing.json` 这种用户机器本地的状态(由 git restore 自动覆盖回 HEAD 版本即可,不另调 rm)

### 1.2 完成判定

| # | 完成判定 | 自检方式 |
|---|---|---|
| C1 | `--dry-run` 模式列出要 restore 的 43 个文件,0 退出,demo output/ 文件无变化 | 跑前后 `md5sum` 任一 demo 文件应一致 |
| C2 | `--yes` 模式后 `git status --short projects/demo_cadre_training/output/` 干净(只剩 raw.txt 等已存在的 untracked) | 命令输出与 §0.3 一致 |
| C3 | `--yes` 后 `.reviewed` 标记仍存在 (红线 1 兼容) | `ls projects/demo_cadre_training/output/tender_brief.reviewed` 存在 |
| C4 | `--yes` 后跑 `parse_tender --force` + `compliance_check` 不报错(ensure_reviewed pass) | 端到端烟测 |
| C5 | 在非仓库根 / 非 demo 项目当前 dir 跑 → 友好报错退出 | 测试用 cd 到 /tmp 再跑 |
| C6 | 1 个单元测试覆盖路径白名单(脚本 hardcode 路径校验) | test fixture 走构造异常路径 |

---

## 2. 实现设计

### 2.1 脚本骨架

```python
# scripts/demo_reset.py
"""V3-13: demo 项目无破坏重跑工具。
restore demo_cadre_training/output/ 下所有 git tracked 文件回 HEAD 状态,
为 parse_tender --force 重跑提供干净 baseline。

红线 1 兼容: 不动 .reviewed 标记(它本就不在 git)、不动 untracked 文件、
不调 git checkout / git clean。
"""
from __future__ import annotations

import argparse
import subprocess
import sys
from pathlib import Path

# Hardcode 路径(V3-13 范围仅 demo_cadre_training)
TARGET_PATH = "projects/demo_cadre_training/output/"
DEMO_REVIEWED_MARKER = "projects/demo_cadre_training/output/tender_brief.reviewed"


def ensure_repo_root() -> Path:
    """确认当前在仓库根。否则 friendly error 退出。"""
    cwd = Path.cwd()
    if not (cwd / ".git").exists() or not (cwd / "scripts").is_dir():
        print(
            f"[错误] demo_reset.py 必须在仓库根跑,当前 cwd={cwd}\n"
            f"提示: 用 ./run_script.bat demo_reset.py 或 cd 到仓库根",
            file=sys.stderr,
        )
        sys.exit(1)
    if not (cwd / TARGET_PATH).is_dir():
        print(
            f"[错误] 找不到目标目录 {TARGET_PATH}\n"
            f"提示: V3-13 仅支持 demo_cadre_training 项目,当前仓库结构异常",
            file=sys.stderr,
        )
        sys.exit(1)
    return cwd


def list_tracked(repo_root: Path) -> list[str]:
    """git ls-files 列出 demo output/ 下所有 tracked 文件。"""
    result = subprocess.run(
        ["git", "ls-files", TARGET_PATH],
        cwd=repo_root, capture_output=True, text=True, encoding="utf-8",
    )
    if result.returncode != 0:
        print(f"[错误] git ls-files 失败: {result.stderr}", file=sys.stderr)
        sys.exit(1)
    return [line for line in result.stdout.splitlines() if line.strip()]


def restore_files(repo_root: Path) -> int:
    """git restore 全部 tracked 文件。返回 git 退出码。"""
    result = subprocess.run(
        ["git", "restore", TARGET_PATH],
        cwd=repo_root, capture_output=True, text=True, encoding="utf-8",
    )
    if result.returncode != 0:
        print(f"[错误] git restore 失败: {result.stderr}", file=sys.stderr)
        return result.returncode
    return 0


def main():
    parser = argparse.ArgumentParser(
        description=(
            "V3-13: demo 项目无破坏重跑工具。restore demo_cadre_training/output/ "
            "回 HEAD 状态,为 parse_tender --force 重跑提供干净 baseline。"
        ),
    )
    g = parser.add_mutually_exclusive_group()
    g.add_argument("--dry-run", action="store_true",
                   help="只显示要做的事,不执行(默认)")
    g.add_argument("--yes", action="store_true",
                   help="确认执行 git restore")
    args = parser.parse_args()

    # 默认 --dry-run(没 --yes 即视为 dry-run)
    is_dry = args.dry_run or (not args.yes)

    repo_root = ensure_repo_root()
    tracked = list_tracked(repo_root)

    print("=" * 60)
    print(f"V3-13 demo_reset: {'DRY-RUN(预览)' if is_dry else 'EXECUTING(执行)'}")
    print("=" * 60)
    print(f"目标目录: {TARGET_PATH}")
    print(f"将 restore {len(tracked)} 个 git tracked 文件 → HEAD 状态")
    print()
    print("范例文件(前 10 条):")
    for f in tracked[:10]:
        print(f"  {f}")
    if len(tracked) > 10:
        print(f"  ... 还有 {len(tracked) - 10} 个文件")
    print()
    print(f".reviewed 标记: 不动({DEMO_REVIEWED_MARKER}, 红线 1 兼容)")
    print(f"untracked 文件: 不动")
    print()

    if is_dry:
        print("[DRY-RUN] 没有执行任何变更。如确认,加 --yes 重跑。")
        sys.exit(0)

    rc = restore_files(repo_root)
    if rc != 0:
        sys.exit(rc)

    # 校验 .reviewed 仍在
    reviewed = repo_root / DEMO_REVIEWED_MARKER
    if not reviewed.exists():
        print(f"[警告] .reviewed 标记不存在 ({reviewed}),需用户手动建后才能跑 parse_tender。")
    else:
        print(f"[确认] .reviewed 标记存在 ({reviewed})")

    print()
    print(f"[完成] {len(tracked)} 个文件已 restore 到 HEAD 状态")
    print()
    print("下一步可选操作:")
    print("  ./run_script.bat parse_tender.py projects/demo_cadre_training/input/tender_demo.docx \\")
    print("      --out projects/demo_cadre_training/output --force")
    sys.exit(0)


if __name__ == "__main__":
    main()
```

### 2.2 依赖确认

- `subprocess` (stdlib): git 调用
- `argparse` (stdlib): CLI
- `pathlib` (stdlib): 路径
- 不需要新 pip 包

### 2.3 红线 1 兼容性声明

| 操作 | 是否安全 | 红线 1 评估 |
|---|---|---|
| `git restore <tracked files>` | ✓ 安全 | 只动 git tracked 文件,与 .reviewed 无关 |
| 不调用 `git checkout` | ✓ | 避免 detach HEAD / 误删 untracked |
| 不调用 `git clean` | ✓ | 避免删 untracked 含 `.reviewed` 风险 |
| 不调用 `rm` / `unlink` | ✓ | 避免误删用户数据 |
| 不创建 / 不写 `.reviewed` | ✓ | 红线 1:用户手动建,AI 不代建 |
| 校验 `.reviewed` 存在 + 提示 | ✓ | 只读校验,不创建 |

---

## 3. 误用防护设计

| 误用场景 | 防护 |
|---|---|
| 在非仓库根跑(如 cd 到 /tmp) | `ensure_repo_root` 校验 .git + scripts/ 存在 |
| 在不存在 demo 目录的仓库跑 | `ensure_repo_root` 校验 TARGET_PATH 是 dir |
| 误把脚本当成"全项目 reset" | hardcode TARGET_PATH = demo_cadre_training,不加 `--project` flag |
| 误以为脚本会重跑 parse_tender | 末尾仅 print 下一步命令,**不调用** parse_tender |
| 默认行为造成"无意触发" | 默认 dry-run,需 `--yes` 显式确认 |
| Windows 路径分隔符 | subprocess 调 git 时 git 自动处理 |

---

## 4. 测试 fixture / 单元测试设计

V3-13 demo_reset 本质是 git wrapper,**单元测试价值低**(mock git 测逻辑而不是行为)。但完成判定要求 ≥1 case (V3-13 plan §3 写)。

折中方案:

### 4.1 单元测试 1 个 (路径白名单 + dry-run 输出)

`scripts/tests/test_demo_reset.py` (新建):

| # | 测试内容 | 期望 |
|---|---|---|
| 1 | 在临时目录(非仓库根)跑 `ensure_repo_root` | 抛 SystemExit (退出码 1),stderr 含"必须在仓库根" |
| 2 | 在仓库根跑 `list_tracked` | 返回 ≥40 个文件路径(43 实测,容忍范围) |
| 3 | dry-run 模式 main(),用 capsys 捕获 stdout | 含 "DRY-RUN" + "没有执行任何变更" |

### 4.2 端到端烟测(Phase 3 完工时手动跑)

```bash
# 1. dry-run 看清单
./run_script.bat demo_reset.py --dry-run
# 期望: 列出 43 个文件,提示"加 --yes 重跑"

# 2. 故意改一个 demo 产物(模拟 --force 跑 parse_tender 后)
echo "DIRTY" >> projects/demo_cadre_training/output/tender_raw.txt

# 3. 跑 demo_reset
./run_script.bat demo_reset.py --yes
# 期望: restore 43 文件,.reviewed 仍在

# 4. 验证 git status 干净
git status --short projects/demo_cadre_training/output/
# 期望: 只剩 raw.txt 等历史 untracked

# 5. 验证 .reviewed 仍存在
ls projects/demo_cadre_training/output/tender_brief.reviewed

# 6. 跑 parse_tender --force 验证 ensure_reviewed pass
./run_script.bat parse_tender.py \
    projects/demo_cadre_training/input/tender_demo.docx \
    --out projects/demo_cadre_training/output --force
# 期望: 解析成功,无 ensure_reviewed 报错
```

---

## 5. Commit 序列(Phase 2 自主执行,1 个 commit)

| # | message | 动到的文件 |
|---|---|---|
| 1 | `feat(v3-13): add demo_reset.py for clean parse_tender re-run` | `scripts/demo_reset.py` (新)+ `scripts/tests/test_demo_reset.py` (新)+ `docs/FAQ.md` (加 Q 条目) |

V3-13 任务短(1-2h),拆 multiple commit 收益小。1 个 commit 包含脚本+测试+FAQ 一并落地。

收尾 commit `docs(v3-13): mark completed` 由用户审过 Phase 3 后再做。

---

## 6. 自检方式(Phase 3 完工时执行)

```bash
# 1. 单元测试
./run_script.bat tests/test_demo_reset.py
# 期望: 3 case PASS

# 2. dry-run 预览
./run_script.bat demo_reset.py --dry-run
# 期望:列 43 个文件,提示加 --yes

# 3. 故意改 demo + reset 端到端
echo "DIRTY" >> projects/demo_cadre_training/output/tender_raw.txt
./run_script.bat demo_reset.py --yes
git status --short projects/demo_cadre_training/output/
# 期望: 只剩历史 untracked

# 4. 跑 parse_tender --force 验证闸门 pass
./run_script.bat parse_tender.py \
    projects/demo_cadre_training/input/tender_demo.docx \
    --out projects/demo_cadre_training/output --force
# 期望: 解析成功;再 demo_reset 一次还原状态

# 5. 在非仓库根跑误用防护
cd /tmp && python /path/to/scripts/demo_reset.py --yes 2>&1
# 期望: stderr 含 "必须在仓库根",退出码 1
```

完工时贴给用户:

1. git diff (1 个 commit)
2. test PASS 报告
3. 5 条烟测输出 (含端到端 reset → parse_tender → reset)
4. C1-C6 逐条对照

---

## 7. 风险

| 风险 | 缓解 |
|---|---|
| 用户在 main 上未 commit 改了 demo,跑 demo_reset 会丢失这些改动 | 风险接受;脚本默认 dry-run + .reviewed 不动;未来加 git stash 集成留 V4 |
| Windows / Linux 路径分隔符差异 | subprocess 调 git CLI,git 自动处理斜杠;TARGET_PATH 用 forward slash 兼容跨平台 |
| git 不在 PATH(开源场景下用户没装 git)| `subprocess.run(["git"...])` 报 FileNotFoundError;脚本捕获后 friendly error |
| demo_reset 之后 .reviewed 已被 V2 hash 校验机制拒绝(虽然 ensure_reviewed 不查 hash) | V3-13 plan §0.1 已实测确认 ensure_reviewed 不查 hash;若未来加 hash 校验,V3-13 需要回滚或更新 |
| 脚本被误用到非 demo 项目(如用户复制脚本改 path) | hardcode 路径 + ensure_repo_root 校验;用户改源码 = 自找,不防 |
| `--dry-run` 和 `--yes` 互斥但默认 `--dry-run` 让用户困惑 | 帮助文本 + 输出明确 "DRY-RUN(预览)" 提示 |

---

## 8. 待用户审核点

- **D0**: CLI 接口 `--dry-run`(默认) / `--yes`(执行),无 `--project` flag(hardcode demo)。OK?
- **D1**: 不做真单元测试 git 行为,只做 ensure_repo_root + list_tracked + dry-run 输出 3 个轻量 case。OK?
- **D2**: 文档化只动 `docs/FAQ.md`(加 1 条 Q),**不动 SKILL.md**(留 V3-10 阶段口径统一时一并处理)。OK?
- **D3**: 1 个 commit 完成全部(脚本 + 测试 + FAQ),不拆细。OK?
- **D4**: `_timing.json` / `_timing_report.md` 这两个 timing 副产物**也包含**在 restore 范围内(它们是 tracked 文件)——重跑 parse_tender 后这俩会被 _timing_hook 重新更新覆盖,demo_reset 不需要特殊处理。OK?
- **D5**: 不调 `git stash` 自动备份用户未 commit 改动(如果有),用户责任自查 git status。OK 还是要加 stash 集成?
- **D6**: 不调 `git clean` 删 untracked(包括用户机器上累积的 raw.txt 等历史遗留)。OK?

审核通过后,我做 Phase 1 闭环 commit + Phase 2 自主 1 个 commit + 完工 Phase 3 审核。
