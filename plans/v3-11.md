# V3-11 执行计划 · B 模式 README + SKILL 口径诚实化

> 本计划是 V3-11 在 Plan-Execute-Review 协议下的 Phase 1 产出。审核通过后进 Phase 2 自主 commit。
>
> V3-11 是 V3-1 的对外口径配套:V3-1 让 B 模式真能命中真实 asset(CuratedLocalAssetsProvider),但 README + SKILL 措辞仍停留在"占位红字标记'待填'"的 v2 时代,触发 R10 精神风险(对外承诺与实际能力分裂)。本计划只动 B 模式措辞,不动 V3-10(五阶段口径)/ V3-12(description 精简)。

---

## 1. 范围与完成判定

**完成判定**(逐条对应自检):

| # | 完成判定 | 自检方式 |
|---|---|---|
| C1 | README 不再出现"非交互批量化:一键跑完所有 C/B 模式 Part,占位红字标记'待填'"原句 | `grep -n "非交互批量化" README.md` 0 命中 |
| C2 | SKILL.md 不再出现"本轮(V61)固定 `placeholder`"等过时事实 | `grep -n "本轮.V61.*placeholder" SKILL.md` 0 命中 |
| C3 | README 描述 B 模式时,新措辞与 V3-1 实际行为一致(从 assets 库按命名约定查找真实材料 + 无命中时占位 + .pending_marker) | 肉眼审 |
| C4 | SKILL.md 阶段 4-B 段反映 placeholder + curated_local 双 provider 并存,CuratedLocalAssetsProvider 标"V3-1 起内置";未来工作只剩 MCPExternalAssetsProvider | 肉眼审 |

**严格不在范围**(scope 红线):

- README L31 / L43 / L49 / L67 涉及"五阶段" — 留 V3-10
- SKILL.md description 字段(顶部 frontmatter)— 留 V3-12
- SKILL.md L693 关于 章节分批 review 节奏("B 模式批量 2-3 章停一次")— 与 V3-1 无关,不动
- SKILL.md L718 / L754 关于 c_mode 占位 — 不是 B 模式,不动
- SKILL.md L818 `.pending_marker` 段 — 现机制 V3-1 没改(无论命中与否始终产 marker),措辞当前仍准,不动
- docs/changelog.md / docs/v2_design_notes.md 等历史档案 — 不动
- 任何 scripts/ 源码 — 不动

---

## 2. 待改清单(grep 出原文 + 行号 + 建议改法)

### README.md

#### R-1:L35 核心能力第 5 条

**原文**:

```markdown
- **非交互批量化**:一键跑完所有 C/B 模式 Part,占位红字标记"待填"
```

**改为**(对齐 V3-1 实际行为 + 保留 C 模式语义):

```markdown
- **B 模式材料组装**:从 assets 库按 asset_type / 公司 id 查找真实材料(CuratedLocalAssetsProvider),合并到 assembled.docx;无命中时产占位 + `.pending_marker`,V45 合并器列入 `pending_manual_work.md`
- **C 模式非交互填充**:一键跑完所有 C 模式 Part,变量缺失时 filled.docx 写入"【待填:字段描述】"显式占位
```

(**拆成两条**,因为 v2 一条句子混了 C 和 B 两个本就不同的机制。V3-11 拆出来,各自描述准确。)

### SKILL.md

#### S-1:L790 `inline_template` 描述

**原文**:

```markdown
- **inline_template**:招标文件给模板 + 变量占位(如基本情况表、信誉声明) → 借用 c_mode 填充能力(当前 B 模式基建阶段产占位段落,未来由 b_mode_fill 自动调 c_mode_fill)
```

**改为**(去掉"基建阶段"措辞,V3-1 没改 inline_template 行为):

```markdown
- **inline_template**:招标文件给模板 + 变量占位(如基本情况表、信誉声明) → 当前产占位段落,未来由 b_mode_fill 自动调 c_mode_fill 完成填充(留 V4)
```

#### S-2:L791 `asset_lookup` 描述

**原文**:

```markdown
- **asset_lookup**:从 assets 库查供应商素材(如资质证明、业绩合同) → 走 `AssetsProvider.lookup + resolve`
```

**改为**(补一句 V3-1 起 curated_local 真实命中):

```markdown
- **asset_lookup**:从 assets 库查供应商素材(如资质证明、业绩合同) → 走 `AssetsProvider.lookup + resolve`;V3-1 起 `CuratedLocalAssetsProvider` 默认扫 `assets/<类别>/<company_id>/_raw/` 真实命中,缺省走 `PlaceholderAssetsProvider` 占位
```

#### S-3:L811-814 `assets_provider 切换` 整段

**原文**:

```markdown
**assets_provider 切换**:

- 本轮(V61)固定 `placeholder`:所有 asset 查询返回占位,resolve 产占位 docx
- 未来接入 `CuratedLocalAssetsProvider` / `MCPExternalAssetsProvider`,切换 manifest 中 `assets_provider` 字段即可,B 模式代码不感知
```

**改为**:

```markdown
**assets_provider 切换**:

manifest.yaml 顶部 `assets_provider` 字段控制本 Part 走哪个 Provider。详见 [docs/manifest_schema.md](docs/manifest_schema.md):

- `placeholder`(V61 基建,默认):所有 asset 查询返回占位,resolve 产占位 docx
- `curated_local`(V3-1 新增):扫 `assets/<类别>/<company_id>/_raw/` 找真实文件,_raw 优先 docx → pdf 走 pdf2docx 转换 → curated `.md` 兜底;多命中走 stdin 交互(`--non-interactive` 时按 `lookup_priority` 自动选);b_mode_fill 自动从 `brief.extracted.bidding_entity` 取 company_id

未来工作:`MCPExternalAssetsProvider`(对接外部材料库,V4+)。
```

---

## 3. Commit 序列(Phase 2 自主执行,预计 1-2 commit)

### 方案 A(推荐):一笔 commit

| # | message | 动到的文件 | 自检 |
|---|---|---|---|
| 1 | `docs(v3-11): align B mode wording in README + SKILL with V3-1 reality` | README.md(R-1)+ SKILL.md(S-1/S-2/S-3)| grep 验证(C1+C2)+ 肉眼审(C3+C4)+ run_all.py 全过 |

### 方案 B(如审核位偏好分文件):2 笔 commit

| # | message | 动到的文件 |
|---|---|---|
| 1 | `docs(v3-11): update README B mode wording to match V3-1 reality` | README.md(R-1) |
| 2 | `docs(v3-11): update SKILL B mode wording to match V3-1 reality` | SKILL.md(S-1/S-2/S-3) |

**我倾向方案 A**:V3-11 整体是单一意图(B 模式口径诚实化),分两笔 commit 反而割裂语义。

---

## 4. 自检方式(Phase 3 完工时执行)

```bash
# C1: 旧措辞清零
grep -n "非交互批量化" README.md     # 应 0 命中

# C2: 旧措辞清零
grep -n "本轮.V61.*placeholder" SKILL.md   # 应 0 命中
grep -n "B 模式基建阶段产占位段落" SKILL.md # 应 0 命中

# C3 / C4: 新措辞已就位
grep -n "CuratedLocalAssetsProvider" README.md SKILL.md  # README ≥1, SKILL ≥1
grep -n "manifest_schema.md" SKILL.md                     # ≥1(链接到 V3-1 新增 doc)

# 不破坏既有测试
./run_script.bat tests/run_all.py    # 退出 0
```

完工时贴给用户:
1. `git diff` 完整输出(README + SKILL 改动行)
2. C1-C4 grep 自检结果
3. run_all.py 退出码

---

## 5. 风险与决策

| 风险 | 缓解 |
|---|---|
| 改 SKILL.md 时不慎触及 V3-10 / V3-12 范围 | 严格按 §2 待改清单执行,改完用 git diff 复核改动行号集合是否在 L790 / L791 / L811-814 + R-1 范围内,若有溢出立即停下 |
| README 拆分核心能力第 5 条为两条后,列表整体长度从 7 项变 8 项,可能影响视觉节奏 | 接受;新措辞精度比列表长度更重要;V3-12 / V3-10 阶段可统筹 README 整体精简 |
| SKILL.md 链接 `docs/manifest_schema.md` 时该路径必须真实存在 | docs/manifest_schema.md 在 V3-1 commit `3a04d9f` 已落地,可直接链 |

---

## 6. 待用户审核点

请用户审核以下决策:

- **D1**:方案 A(单 commit)还是方案 B(分 README / SKILL 两 commit)?(我推荐 A)
- **D2**:R-1 把核心能力第 5 条拆成两条(B 模式材料组装 + C 模式非交互填充),是否 OK?(替代:维持一条但写得更准确,但混述两个机制不够清晰)
- **D3**:S-1 改 inline_template 描述时把"基建阶段"措辞去掉,但保留"留 V4"标注,你 OK 吗?(替代:整段重写,但当前实现 V3-1 没改 inline_template,描述本身仍准,只是措辞过期)

审核通过后,我做 Phase 1 闭环 commit(plans/v3-11.md 入库)→ Phase 2 自主执行(1 个或 2 个 commit)→ 完工后贴 Phase 3 审核。
