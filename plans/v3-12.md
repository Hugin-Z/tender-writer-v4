# V3-12 执行计划 · SKILL.md description 字段精简

> 本计划是 V3-12 在 Plan-Execute-Review 协议下的 Phase 1 产出。审核通过后进 Phase 2 自主 commit。
>
> V3-12 是 1h 文案小活——SKILL.md frontmatter description 字段触发关键词清单从当前数量精简到核心 10 个,降低 Claude Code 过触发风险。
>
> Plan 阶段已用 .venv python 切关键词清单(对齐 feedback_phase0_paste_command_output **应用要点强化版**:三次数字漂移后,本期任何数字声明前必须真跑 wc -l / python len / re.split,实测对不上立即改 plan 不改实测)。

---

## 0. Phase 0 关键发现(实测命令输出贴 plan,数字全跑)

### 0.1 description 字段全文实测

```bash
$ .venv/Scripts/python.exe -c "
import re
text = open('SKILL.md', encoding='utf-8').read()
fm = re.search(r'^---\s*\n(.*?)\n---', text, re.DOTALL).group(1)
desc = re.search(r'^description:\s*(.+)\$', fm, re.MULTILINE).group(1).strip()
print(f'description 总字符: {len(desc)}')
print(desc)
"
description 总字符: 315
```

description 字段全文(本 plan 落 §3 diff 时同样这段):

```text
政府类项目投标文件(技术标)编制专家。当用户上传招标文件、招标公告、
采购文件,或提到投标、技术标、响应文件、投标方案、技术方案应答、
评分办法、废标条款、实质性响应、资格要求、政府采购、竞争性磋商、
单一来源采购、技术标书、商务标、投标书、投标响应文件、应答文件、
标书编制、标书撰写、采购需求应答、招投标文件解读、评标办法解析
等关键词时触发。本 skill 严格按照"招标文件解析→评分矩阵构建→提纲
生成→分章节撰写→合规终审"五阶段主干 + 并列阶段(4-B/4-C/6)工作流
推进,确保每一分都对应到具体应答内容,杜绝漏答、错答、废标风险。
适用于政府机关、事业单位、国有企业的信息化、智慧城市、数字乡村、
政务系统等技术标编制场景。
```

### 0.2 关键词数实测(分两类:前置 + 触发)

```bash
$ .venv/Scripts/python.exe -c "
import re
desc = open('tmp/desc.txt', encoding='utf-8').read()
# 触发段:'或提到 X 等关键词时触发'
m = re.search(r'或提到(.+?)等关键词时触发', desc)
trigger_seg = m.group(1)
trigger_kws = [k.strip() for k in re.split(r'[、,，]', trigger_seg) if k.strip()]
print(f'触发关键词数: {len(trigger_kws)}')
# 前置段:'当用户上传 X' 后到'或'前
m2 = re.search(r'当用户上传(.+?),或', desc)
preset_seg = m2.group(1)
preset_kws = [k.strip() for k in re.split(r'[、,，]', preset_seg) if k.strip()]
print(f'前置关键词数: {len(preset_kws)}')
print(f'总关键词数: {len(trigger_kws) + len(preset_kws)}')
"
触发关键词数: 22
前置关键词数: 3
总关键词数: 25
```

**与 plan 现状描述对比**:

| 数字 | docs/v3_planning.md L239 写 | 实测 |
|---|---|---|
| 触发关键词总数 | "40+ 触发关键词" | **25**(前置 3 + 触发 22)|

**v3_planning.md 写"40+"是历史估算,实际只 25**。本 plan **以实测 25 为准**(对齐 feedback 强化版"实测对不上立即改 plan 不改实测")。

### 0.3 关键词全清单(命令输出原样)

**前置 3 个**(`当用户上传` 段):

```
1  招标文件
2  招标公告
3  采购文件
```

**触发 22 个**(`或提到` 段):

```
 1  投标
 2  技术标
 3  响应文件
 4  投标方案
 5  技术方案应答
 6  评分办法
 7  废标条款
 8  实质性响应
 9  资格要求
10  政府采购
11  竞争性磋商
12  单一来源采购
13  技术标书
14  商务标
15  投标书
16  投标响应文件
17  应答文件
18  标书编制
19  标书撰写
20  采购需求应答
21  招投标文件解读
22  评标办法解析
```

### 0.4 触发精度评估(逐词)

| # | 关键词 | 精度 | 理由 | 决策 |
|---|---|---|---|---|
| 前 1 | 招标文件 | 高 | 招投标核心,单义 | **保留** |
| 前 2 | 招标公告 | 高 | 招投标核心 | **保留** |
| 前 3 | 采购文件 | 中 | 与"招标文件"近似冗余 | 删除 |
| 1 | 投标 | 高 | 核心动作词 | **保留** |
| 2 | 技术标 | 高 | 招投标专属 | **保留** |
| 3 | 响应文件 | 高 | 招投标专属(招标文件→响应文件) | **保留** |
| 4 | 投标方案 | 中 | 与"投标"重叠 | 删除 |
| 5 | 技术方案应答 | 中 | "技术方案"在 IT/工程领域常见,易误触发 | 删除 |
| 6 | 评分办法 | 高 | 招投标专属 | **保留** |
| 7 | 废标条款 | 高 | 招投标专属,用户高频搜索词 | **保留** |
| 8 | 实质性响应 | 高 | 招投标专属术语 | **保留** |
| 9 | 资格要求 | 低 | 通用 HR 词,易误触发 | 删除 |
| 10 | 政府采购 | 高 | 政府类项目核心 | **保留** |
| 11 | 竞争性磋商 | 高 | 政府采购专属采购方式 | **保留** |
| 12 | 单一来源采购 | 中 | 与"政府采购"+"竞争性磋商"覆盖重复 | 删除 |
| 13 | 技术标书 | 中 | 与"技术标"冗余 | 删除 |
| 14 | 商务标 | 中 | 范围外(本 skill 只做技术标) | 删除 |
| 15 | 投标书 | 中 | 与"投标"冗余 | 删除 |
| 16 | 投标响应文件 | 中 | 与"响应文件"冗余复合词 | 删除 |
| 17 | 应答文件 | 中 | 与"响应文件"近似冗余 | 删除 |
| 18 | 标书编制 | 中 | 与"投标"动作冗余 | 删除 |
| 19 | 标书撰写 | 中 | 与"标书编制"冗余 | 删除 |
| 20 | 采购需求应答 | 低 | 复合词,易拆分误触发"采购需求" | 删除 |
| 21 | 招投标文件解读 | 低 | 复合词,与"招标文件"冗余 | 删除 |
| 22 | 评标办法解析 | 中 | 与"评分办法"近似冗余 | 删除 |

**汇总(实测)**:
- 保留 = **10**(前置 2 + 触发 8)
- 删除 = **15**(前置 1 + 触发 14)
- 25 - 15 = 10 ✓

---

## 1. 范围与完成判定

### 1.1 范围

**做**:

- 改 SKILL.md frontmatter `description` 字段:
  - 前置段从 3 个砍到 2 个(删"采购文件")
  - 触发段从 22 个砍到 8 个(删 14 个,见 §0.4 表)
  - description 后半句子(`本 skill 严格按照...场景` 那段)**不动**(已含 V3-10 改的"五阶段主干 + 并列阶段(4-B/4-C/6)"措辞)

**不做**:

- 不动 description 后半句子(避开 V3-10 改的措辞,不冲突)
- 不动 SKILL.md frontmatter 之外任何内容(只改 frontmatter description 行)
- 不动 SKILL.md 正文章节
- 不引入新关键词(只删不加)
- 不改 README.md / .clinerules / 其他文档(本期范围内只改 SKILL.md description)

### 1.2 完成判定

| # | 完成判定 | 自检方式 |
|---|---|---|
| C1 | 触发关键词总数从 25 砍到 10(保留 10 个,删除 15 个) | python re.split 重切,断言 len = 10 |
| C2 | 保留的 10 个关键词字面在新 description 中 | python `for kw in keep: assert kw in desc` |
| C3 | 删除的 15 个关键词字面**不在**新 description 中 | python `for kw in drop: assert kw not in desc` |
| C4 | description 后半句子(`本 skill 严格按照...场景`) 0 改动 | git diff 看只动前半段 |
| C5 | V3-10 措辞"五阶段主干 + 并列阶段(4-B/4-C/6)"在新 description 中保留 | grep "五阶段主干 + 并列阶段(4-B/4-C/6)" SKILL.md 命中 |
| C6 | 模拟用户场景测精简后仍能触发 | 自检 §6 用 5 个真实场景测 |
| C7 | description 总字符数从 315 显著缩短(预估 ~200) | python len 测,贴前后对比 |

---

## 2. 删除 / 保留清单(精确决策)

### 2.1 保留 10 个

```
前置(2):
  1  招标文件
  2  招标公告

触发(8):
  3  投标
  4  技术标
  5  响应文件
  6  评分办法
  7  废标条款
  8  实质性响应
  9  政府采购
 10  竞争性磋商
```

**保留理由汇总**:
- 招标领域专属词(招标文件 / 招标公告 / 投标 / 技术标 / 响应文件 / 评分办法 / 废标条款 / 实质性响应)
- 政府场景核心词(政府采购 / 竞争性磋商)
- 覆盖招标→投标→响应→评分→废标 全链路词汇,不堆冗余别名

### 2.2 删除 15 个

```
前置删除(1):
  采购文件        理由: 与"招标文件"冗余

触发删除(14):
  投标方案        理由: "投标"已覆盖
  技术方案应答    理由: "技术方案"在 IT/工程领域常见,易误触发
  资格要求        理由: 通用 HR 词,误触发
  单一来源采购    理由: "政府采购"+"竞争性磋商"已覆盖
  技术标书        理由: 与"技术标"冗余
  商务标          理由: 范围外(本 skill 只做技术标)
  投标书          理由: 与"投标"冗余
  投标响应文件    理由: 与"响应文件"冗余复合词
  应答文件        理由: 与"响应文件"近似冗余
  标书编制        理由: 与"投标"动作冗余
  标书撰写        理由: 与"标书编制"冗余
  采购需求应答    理由: 易拆分误触发"采购需求"
  招投标文件解读  理由: 复合词与"招标文件"冗余
  评标办法解析    理由: 与"评分办法"近似冗余
```

---

## 3. description 字段改前后 diff

### 3.1 改前(原 description,315 字符)

```text
政府类项目投标文件(技术标)编制专家。当用户上传招标文件、招标公告、
采购文件,或提到投标、技术标、响应文件、投标方案、技术方案应答、
评分办法、废标条款、实质性响应、资格要求、政府采购、竞争性磋商、
单一来源采购、技术标书、商务标、投标书、投标响应文件、应答文件、
标书编制、标书撰写、采购需求应答、招投标文件解读、评标办法解析
等关键词时触发。本 skill 严格按照"招标文件解析→评分矩阵构建→提纲
生成→分章节撰写→合规终审"五阶段主干 + 并列阶段(4-B/4-C/6)工作流
推进,确保每一分都对应到具体应答内容,杜绝漏答、错答、废标风险。
适用于政府机关、事业单位、国有企业的信息化、智慧城市、数字乡村、
政务系统等技术标编制场景。
```

### 3.2 改后(精简到 10 个关键词,预估 ~250 字符)

```text
政府类项目投标文件(技术标)编制专家。当用户上传招标文件、招标公告,
或提到投标、技术标、响应文件、评分办法、废标条款、实质性响应、政府
采购、竞争性磋商等关键词时触发。本 skill 严格按照"招标文件解析→
评分矩阵构建→提纲生成→分章节撰写→合规终审"五阶段主干 + 并列阶段
(4-B/4-C/6)工作流推进,确保每一分都对应到具体应答内容,杜绝漏答、
错答、废标风险。适用于政府机关、事业单位、国有企业的信息化、智慧
城市、数字乡村、政务系统等技术标编制场景。
```

### 3.3 改后实测字符数(待 Phase 2 落地后真测)

预估 ~250 字符,Phase 2 commit 1 落地后跑 python len 测真值贴 commit message。

### 3.4 V3-10 措辞保护(C5 关键)

新 description 中"五阶段主干 + 并列阶段(4-B/4-C/6)" 完整保留(diff 后半段 0 改动)。**不冲突 V3-10**。

---

## 4. Commit 序列(Phase 2 自主执行,1 个 commit)

| # | message | 动到的文件 |
|---|---|---|
| 1 | `docs(v3-12): trim SKILL.md description from 25 to 10 trigger keywords` | `SKILL.md` (只改 frontmatter description 行) |

V3-12 是纯文案 1h 短任务,1 个 commit 全部落地。收尾 commit `docs(v3-12): mark completed` 由用户审过 Phase 3 后再做。

---

## 5. 自检方式(Phase 3 完工时执行)

### 5.1 关键词清单 grep 验证(C1-C3)

```bash
.venv/Scripts/python.exe -c "
import re
text = open('SKILL.md', encoding='utf-8').read()
fm = re.search(r'^---\s*\n(.*?)\n---', text, re.DOTALL).group(1)
desc = re.search(r'^description:\s*(.+)\$', fm, re.MULTILINE).group(1).strip()

# C1: 触发段关键词数 = 8(触发) + 2(前置) = 10
preset_seg = re.search(r'当用户上传(.+?),或', desc).group(1)
trigger_seg = re.search(r'或提到(.+?)等关键词时触发', desc).group(1)
preset = [k.strip() for k in re.split(r'[、,，]', preset_seg) if k.strip()]
trigger = [k.strip() for k in re.split(r'[、,，]', trigger_seg) if k.strip()]
print(f'前置 {len(preset)}: {preset}')
print(f'触发 {len(trigger)}: {trigger}')
print(f'总数: {len(preset) + len(trigger)}')
assert len(preset) + len(trigger) == 10, '总数应为 10'

# C2: 保留 10 个全部在
keep = ['招标文件','招标公告','投标','技术标','响应文件','评分办法','废标条款','实质性响应','政府采购','竞争性磋商']
for k in keep:
    assert k in desc, f'保留词缺失: {k}'

# C3: 删除 15 个全部不在
drop = ['采购文件','投标方案','技术方案应答','资格要求','单一来源采购','技术标书','商务标','投标书','投标响应文件','应答文件','标书编制','标书撰写','采购需求应答','招投标文件解读','评标办法解析']
for k in drop:
    assert k not in desc, f'删除词残留: {k}'

print('OK: C1+C2+C3 全过')
"
```

### 5.2 V3-10 措辞保护 + description 后半段不动(C4 + C5)

```bash
grep -c "五阶段主干 + 并列阶段(4-B/4-C/6)" SKILL.md
# 期望: ≥ 1(L3 description 行)

git diff SKILL.md
# 期望: 只改 L3 description 一行内的"或提到 X 等关键词" 那段
#       本 skill 严格按照... 那段 0 改动
```

### 5.3 字符数对比(C7)

```bash
.venv/Scripts/python.exe -c "
import re
text = open('SKILL.md', encoding='utf-8').read()
desc = re.search(r'^description:\s*(.+)\$', text, re.MULTILINE).group(1).strip()
print(f'改后字符数: {len(desc)}')
"
# 期望: 显著少于 315(改前)
```

### 5.4 真实场景触发模拟(C6)

模拟 5 个用户问句,看精简后 description 是否仍能触发(关键词命中):

| # | 模拟用户问句 | 应命中关键词 | 期望 |
|---|---|---|---|
| 1 | "帮我看下这份招标文件,要写技术标" | 招标文件 / 技术标 | ✓ |
| 2 | "政府采购竞争性磋商怎么写响应文件" | 政府采购 / 竞争性磋商 / 响应文件 | ✓ |
| 3 | "评分办法怎么对照,有没有废标条款风险" | 评分办法 / 废标条款 | ✓ |
| 4 | "投标书的实质性响应怎么写" | 投标 / 实质性响应 | ✓ |
| 5 | "看下招标公告" | 招标公告 | ✓ |

**反向场景**(精简后**不应**触发的):

| # | 模拟用户问句 | 删除关键词 | 期望 |
|---|---|---|---|
| 1 | "帮我整理资格要求" | 资格要求 → 已删除 | 不直接命中(精简成功)|
| 2 | "我们做技术方案的应答" | 技术方案应答 → 已删除 | 不直接命中 |

**注**: Claude Skill 触发是 LLM 端语义判断,不是关键词字面 match。本期 description 改后仍含语义信号("政府类项目投标文件 / 技术标 / 招标"等),实际触发由 Claude 模型决策,关键词清单只是辅助提示。模拟测仅作字面命中验证,不是端到端触发测试。

---

## 6. 风险

| 风险 | 缓解 |
|---|---|
| 砍掉某个常用关键词导致用户场景漏触发(如招标方说"标书撰写"被删) | 保留 10 个覆盖招标全链路核心词;真实场景如发现漏触发,Phase 3 调整或登记 V4;Claude 端语义触发不依赖字面 match 强 |
| description 改后 Claude Skill 加载行为变化(skill 不再被识别) | description 长度变化但 frontmatter 结构不变,加载机制无变化;Claude Skill 描述精简通常提升触发精度不是降低 |
| V3-10 措辞"五阶段主干 + 并列阶段(4-B/4-C/6)"被本期误删 | C5 完成判定显式 grep 验证;§3.2 改后样本明确保留这段措辞 |
| description 后半段(`本 skill 严格按照...场景`) 误改 | C4 git diff 验证只改前半段;§3 diff 表对照新旧 |
| 触发段切词 regex 匹配错(如新 description 顿号格式与旧不同导致 wc -l 错) | §5.1 自检直接 python re.split 重切,断言 len = 10;格式问题立即暴露 |

---

## 7. 待用户审核点

请用户审核以下决策:

- **D0**: 实测纠正:`v3_planning.md` L239 写"40+ 关键词",实测 25 个(前 3 + 触发 22)。本 plan **以 25 为准**改成 10 个(不是 plan 现状的 40+→20)。OK?
- **D1**: 保留 10 个清单(§2.1):招标文件 / 招标公告 / 投标 / 技术标 / 响应文件 / 评分办法 / 废标条款 / 实质性响应 / 政府采购 / 竞争性磋商。OK?
- **D2**: 删除 15 个清单(§2.2):每个删除理由见表(冗余 / 易误触发 / 范围外)。OK?
- **D3**: description 后半段(`本 skill 严格按照...场景`) 0 改动,V3-10 措辞完整保留。OK?
- **D4**: 1 个 commit 完成,不拆细。OK?
- **D5**: 真实触发测仅作字面命中验证(§5.4),不做端到端测(Claude Skill 触发是 LLM 语义判断,字面 match 只是辅助)。OK?
- **D6**: V3-12 不写单元测试(纯 frontmatter 改,无新代码)。OK?

审核通过后,我做 Phase 1 闭环 commit + Phase 2 自主 1 个 commit + 完工 Phase 3 审核。
