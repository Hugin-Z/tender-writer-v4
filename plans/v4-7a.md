# plans/v4-7a.md — V4-7a 合并器浅做接 master 规范 (font + cell sz 两维 done)

## 任务定义

V4-7 拆 **V4-7a (浅做 / 现在做) + V4-7b (深 / 占位押后等真实标驱动)**。

V4-7a 解 docxcompose "后到优先" 导致的**两个确定问题** (跑真实标必看到):
1. anchor 自带 Heading 蓝色 (4F81BD) 注入 master styles 覆盖 master 000000 黑
2. 合并后表格 cell 字号回归 Normal 14pt,不符 V4-1a 表格小一号 12pt 约定

**浅做 = 接 master 现成规范 apply 一次 + 整篇 cell sz once**(~30 行 / 复用 V4-1a 现成 helper / 0 新 OXML 操作):
- `apply_default_styles(final_doc)` (style 层)
- `_apply_table_cell_size_to_runs(final_doc.element)` (整篇 cell run / 复用 V4-1a.12 helper)

## Phase 0 实测发现 (实测坐实思路成立)

模拟 V45 合并 V4-1a anchor 1.docx 跑 _post_merge_normalize 后产物实测:

| 维度 | apply 前 (docxcompose 默认) | V4-7a 浅做后 |
|---|---|---|
| Heading2 color | `4F81BD` 蓝 (anchor 自带 styles 注入 master) | `000000` 黑 ✓ (apply 压回) |
| Normal sz | `no` (空 master 默认) | `28` (14pt apply 后) ✓ |
| TableNormal sz | `no` | `no` (V4-1a.12 已删死代码,table_cell_size 用 per-run set) |
| 表格 cell run sz | 源 anchor 自带 (`24` per-run 真值, 但其他无 sz 的表会落 Normal 14pt) | 全篇 cell run sz=24 (V4-7a 强 set 整篇) ✓ |
| run-level direct sz/color | 103 处 sz / 0 color (anchor 自带) | 103 处不变 (apply 不剥 run-level / V4-7b 押后) |

**关键事实**:
- style 层 apply 能解 Heading 蓝色 + Normal 字号 (style 层 OK)
- 表格 cell 字号靠 V4-1a.12 helper 强 set 整篇 cell run sz=24 (run-level 优先级最高,XML 有则 WPS 必取)
- run-level direct 不变 (per-run normalize 整篇 = V4-7b 深, V4-1a 量级 cost, 押后等真实标)

## 已定裁定 (Phase 1 ack)

1. **5 维度诚实摊开** (字段名精准对齐行为, R10 红线, 沿 V4-1a.10 教训):
   - `font_normalize`: done (style-level) ← V4-7a 浅做
   - `table_cell_size`: done (N cell run set) ← V4-7a 浅做 / **新增维度**
   - `table_width_unify`: noop ← V4-7b 押后 (列宽统一)
   - `section_break`: noop ← V4-7b 押后
   - `page_number_reset`: noop ← V4-7b 押后
2. **不沿用 "table_width_unify: done (cell sz only)"** (字段名误导:机器读 key 不读括号说明, V4-1a.10 同型 R10)
3. **复用 V4-1a.12 `_apply_table_cell_size_to_runs`** 整篇 doc (helper 本身 `elem.iter(qn("w:tbl"))` 已是全遍历, scope 直接扩, 0 helper 改动)
4. **测试验生效层** (沿 V4-1a 教训): case_9 招牌验合并产物 OXML 字段值 (Heading2 color=000000 / cell run sz=24 / Normal sz=28), 不只验 helper 调了 apply

## scope 边界 (钉死)

**动**:
- `scripts/v45_merge.py` (`_post_merge_normalize` body + `_write_merge_normalize_log` helper 5 维度 + 头注诚实化)
- `scripts/tests/test_v4_skeleton_e2e.py` (case_6/7/8 改写 + case_9 新增)
- `scripts/tests/r10_allowlist.yaml` (`plans/v4-7a.md` future_file 登记)
- `SKILL.md` L840 (V4-skel.9 字面后追加 V4-7a 段)
- `docs/changelog.md` (新 V4-7a entry 插顶部)
- `docs/v4_backlog.md` §1 V4-7 状态行替换
- `plans/v4-7a.md` (本 plan, 入仓)

**不动** (越界即停):
- V4-1a.12 `_apply_table_cell_size_to_runs` helper 实现 (只复用, 0 改)
- V4-1a 其他 helper (`_apply_explicit_fonts_to_runs` / `_normalize_run_size` / `_rebind_or_strip_refs`)
- V4-2a / V4-2b / V4-1b / V4-skel 现有逻辑
- `_post_merge_normalize` 接入点位置 (V4-skel.7 已接好, 内部 body 改)
- section break / page number 协调 (V4-7b)
- 整篇 per-run normalize (V4-7b)
- master DEFAULT_STYLES / DEFAULT_TABLE_SIZE_PT 定义
- 新 OXML 操作 / 新 helper
- handbook 仓

## commit 序列 (3 commits + 1 子 fix commit)

| # | commit hash | message | 文件 |
|---|---|---|---|
| V4-7a.1 | cdc1cf4 | `_post_merge_normalize 接 apply_default_styles + 整篇 cell sz set (style 层 + 表格 cell)` | v45_merge.py |
| V4-7a.1b | 2b56ec3 | `_write_merge_normalize_log helper 同步 5 维度 + 文件头注诚实化` (V4-7a.1 漏改 helper, 现场目检 log 发现 4 维度 + "全 noop" 头注跟实际 5 维度 2 done 不符, 沿 V4-1a 验生效教训补) | v45_merge.py |
| V4-7a.2 | 5b32b8d | `测试改写验生效:合并产物 Heading 黑 + 表格 cell sz=24 (沿 V4-1a 验生效教训)` | test_v4_skeleton_e2e.py / r10_allowlist.yaml |
| V4-7a.3 | (本 commit) | `SKILL/changelog/v4_backlog 字面诚实化 + plan 入仓` (Hugin 定稿字面) | SKILL.md / docs/changelog.md / docs/v4_backlog.md / plans/v4-7a.md |

V4-7a.1b 是 V4-7a.1 现场补 (helper 漏改实测发现), commit message 内透明 disclosure。

## 实现指令 (Phase 2 已落 V4-7a.1/.1b/.2, 本 plan 由 V4-7a.3 落盘)

### V4-7a.1 — `_post_merge_normalize` body

```python
def _post_merge_normalize(final_doc, fragments_info: list[dict]) -> dict:
    # V4-7a 浅做 step 1: style 层 apply
    from docx_builder import apply_default_styles
    apply_default_styles(final_doc)
    # V4-7a 浅做 step 2: 整篇 <w:tbl> cell run 强 set sz/szCs=24
    from b_mode_fill import _apply_table_cell_size_to_runs
    cell_size_set_total = _apply_table_cell_size_to_runs(final_doc.element)
    result = {
        'fragments_count': len(fragments_info),
        'font_normalize': 'done (style-level)',
        'table_cell_size': f'done ({cell_size_set_total} cell run set)',
        'table_width_unify': 'noop',
        'section_break': 'noop',
        'page_number_reset': 'noop',
    }
    print(f"[V4-7a 浅做] post_merge_normalize: {result}", file=sys.stderr)
    return result
```

延迟 import 跟 v45_merge 现有 `_create_part_divider` / `_create_inapplicable_doc` 内 `apply_default_styles` 调用同型。

### V4-7a.1b — `_write_merge_normalize_log` helper

5 维度 + 头注从 "V4-7 结构占位 / 全 noop" 改为 "V4-7a 浅做 / V4-7a 完成 2 维 / V4-7b 押后 3 维"。

### V4-7a.2 — 测试改写 + case_9 招牌

- case_6 改写: 验返 5 维度 (2 done + 3 noop) + stderr [V4-7a 浅做] 喊话
- case_7 改写: 验 log 5 维度 + 头注 V4-7a 浅做 + V4-7b 押后
- case_8 改写: V4-7 dict 从 4 noop 改 5 维 (2 done + 3 noop)
- case_9 新增 V4-7a 招牌 (沿 V4-1a 验生效教训): anchor 1.docx 合并端到端实测合并产物 OXML 字段值 — Heading2 color=000000 + cell run sz=24 全篇 + Normal sz=28

### V4-7a.3 — 字面诚实化 (本 commit)

- SKILL.md L840 追加 V4-7a 段 (不动 V4-skel.9 现有字面)
- docs/changelog.md 新 V4-7a entry 插顶部 (V4-skel entry 之前)
- docs/v4_backlog.md §1 V4-7 状态行替换 ("结构层已补 (V4-skel)" → "V4-7a 浅做完 / V4-7b 深押后")
- plans/v4-7a.md (本 plan) 入仓

## 完成判定 (6 条 / Phase 3 自检)

1. **`_post_merge_normalize` body 改对**: 不再是全 noop, 含 `apply_default_styles(final_doc)` + `_apply_table_cell_size_to_runs(final_doc.element)` 两个调用
2. **style 层生效** (沿 V4-1a 验生效): case_9 实测合并产物 styles.xml Heading2 color=000000 (anchor 自带蓝色 4F81BD 已被压)
3. **表格 cell sz 生效**: case_9 实测合并产物内 `<w:tbl>` cell run rPr 含 `<w:sz w:val="24"/>` (12pt 约定不回归 Normal 14pt) + Normal sz=28
4. **log 字面同步**: merge_normalize.log 5 维度 + 头注 "V4-7a 浅做" + "V4-7a 完成 2 维 / V4-7b 押后 3 维" + `font_normalize: done (style-level)` + `table_cell_size: done (N)` + 3 维 noop
5. **scope 零越界**: git diff --name-only 仅 plan §scope 动文件清单 / V4-1a helper 0 改 / 新 OXML 操作 / section / page_number 0 动
6. **V4-skel / V4-1a / V4-2a / V4-1b / V4-2b 不回归**: run_all.py 16/16 PASS + R10 基线持平 (allowlist 外零违规)

## 关键约束 (Phase 2)

- 每 commit 后跑 run_all.py + R10 scan
- V4-7a.1 后用 V4-1a anchor 1.docx 模拟合并生成 demo 产物 (`_v4_7a_phase3_merged.docx` + `_v4_7a_phase3_merge_normalize.log` 固定路径) 供 Hugin 视觉目检 (不入仓, 跟 V4-1a / V4-skel C10 同型)
- V4-7a.3 SKILL/changelog/v4_backlog 字面落盘前停下问 Hugin
- 硬停下:
  - 四种合法停下情形
  - apply_default_styles 接整篇 doc 不生效 → 停下报告
  - `_apply_table_cell_size_to_runs` 接 final_doc.element 不生效 / 误触表外段 → 停下报告
  - 测试 case_6/7 改写发现 V4-skel.7 原 assertion 跟 V4-7a 行为冲突 → 停下报告

## V4-7b 押后摊开 (本任务不做, 防下个会话误以为 V4-7 整个完成)

V4-7b 三维实线协调留实现层, 待真实多 Part 合并产物视觉冲突驱动:
- **`table_width_unify`** (列宽统一): 跨 Part 表格列宽统一规则, 待真实多 Part 合并产物视觉冲突定 (V4-1a 单 doc deepcopy 自带列宽已对, 跨 Part 是否要统一未知)
- **`section_break`** (section 收敛): docxcompose 默认每 fragment 一 section, 是收敛为全局单 section 还是 section restart numbering 显式控制, 待真实标视觉判
- **`page_number_reset`** (页码协调): 跨 fragment 页码连续 / restart, 待真实标视觉判
- **per-run normalize 整篇** (跟 V4-7a font_normalize style 层互补): 剥整篇 doc run-level direct sz/color/字体 + 重 set master 值, 跟 V4-1a `_normalize_run_size` + `_apply_explicit_fonts_to_runs` 同型, 跨 A/B/C 模式协调可能更深 (单 Part V4-1a.9-.12 = 4 commits / 6 轮 bug, V4-7b 量级 × 跨 Part 复杂度)

**做不做 V4-7b 取决于 V4-7a 浅做完视觉够不够** — 不可假设 V4-7 整个完成。
