# V4-0 执行计划 · business_model 文档化 + R10 扫描升级

> 本计划是 V4-0 在 PER (Plan-Execute-Review) 协议下的 Phase 1 产出定稿版.
> Phase 1 plan ack 后进 Phase 2 (落盘 7 commits) → Phase 3 (供审).
>
> V4-0 是 V4 首项, 解 audit-2026-05-27 §5 #1 (业务模型文档 14+2 处空指针)
> + V4-0.2 R10 扫描升级 + 修剩余 #2 / #3 / #5 / #6 + 新发现的违规.
>
> **不动业务代码逻辑**, 只动文档 / 注释 / 文案 + 新增扫描脚本 + fixture.

---

## §1 V4-0.1 · docs/business_model_v1.md 文档化

### 1.1 精确空指针清单 (Phase 0 grep 实测)

16 处显式 `business_model` 引用 + 2 处隐含 `#NXX` 引用 (无 business_model 前缀):

| 来源 | 行 | 引用章节 | 语义 |
| --- | --- | --- | --- |
| brief_schema.py | L27 | §8.3 #N16 + §9 | REGEX_DRAFT 删除决策 + skill 模型声明 |
| brief_schema.py | L102 | §8 #N20 | SUB_MODE_JUDGE_PROMPT 迁移目的地 |
| brief_schema.py | L103 | §9 | skill 模型声明 |
| brief_schema.py | L121 | §8 #N20 | sub_mode 判据 (ValueError 文案) |
| brief_schema.py | L149 | §8 #N21 | B_MODE_EXTRACT_PROMPT 迁移目的地 |
| c_mode_extract.py | L346 | §8 #N20 | C-attachment 挂档 |
| c_mode_fill.py | L325 | §8 #N20 | C-attachment 挂档 |
| export_deliverables.py | L10 | §8 #N24 | 交付层独立于工具链内部 |
| export_deliverables.py | L193 | §9 | 主 agent 做语义结构化 |
| migrate_brief_schema.py | L152 | §8 #N20 | sub_mode 判据 (docstring) |
| migrate_brief_schema.py | L174 | §8 #N20 | sub_mode 判据 (ValueError) |
| migrate_brief_schema.py | L185 | §8 #N20 | C-attachment 挂档 |
| parse_tender.py | L301 | §8.3 #N16 + §9 | REGEX_DRAFT 删除决策 |
| parse_tender.py | L500 | §8.3 #N16 + §9 | REGEX_DRAFT 删除决策 (代码注释) |
| parse_tender.py | L847 | §9 | print 文案 |
| parse_tender.py | L849 | §8 #N20 | print sub_mode 判据 |
| SKILL.md | L763 / L771 | §8 #N19 | 完整重做触发 (隐含, 无前缀) |
| v45_merge.py | L84 / L276 | §8 #N22 | C-reference 不进 final_response.docx (隐含, 无前缀) |

### 1.2 文档结构 outline

```
# tender-writer business model (v1)

⚠️ R10 诚实声明 (文档头部)
  本文档定位: V4-0 期间为解 18 处空指针从 SKILL.md + 脚本注释反推沉淀的
  "引用锚点集", 不是完整业务模型权威文档. 只对被引用的 §X #NXX 字面负责,
  不保证业务模型整体完整性. #N17 / #N18 / #N23 等跳号在仓内无引用 / 留白
  不补造. 文档化过程中若发现某 §X #NXX 字面跟代码行为不符, 已在对应章节
  标注 [字面-行为 gap] / 未自行裁定改字面还是改代码.

§1 文档定位
§8 历史决策快照
  §8.3 #N16 · __REGEX_DRAFT__ 机制删除
  §8 #N19 · Part 完整重做触发条件
  §8 #N20 · sub_mode 判据 + C-attachment 挂档
  §8 #N21 · B 模式 assembly_order 组织判据
  §8 #N22 · C-reference 不进 final_response.docx
  §8 #N24 · 交付层独立于工具链内部
§9 skill 模型声明
```

### 1.3 不动

- 16 处显式 + 2 处隐含 business_model 引用注释 全部保留原文不改
- V4-0.1 让引用从空指针变实指针, 引用本身不动

---

## §2 V4-0.2 · R10 扫描升级 + 修剩余违规

### 2.1 扫描脚本设计 (`scripts/tests/r10_consistency_check.py`)

**输入**: 仓库内 `.py` / `.md` / `.yaml` / `.yml` 文件 (排除 `.git` / `.venv` /
`__pycache__` / `node_modules` / `.idea` / `.vscode` + EXCLUDE_FILENAMES
`_r10_report.md` + EXCLUDE_PATH_PREFIXES `scripts/tests/fixtures/`)

**扫描点 (L1 v1 → v2 升级)**:

| 类型 | v1 | v2 (本次 V4-0.2) |
| --- | --- | --- |
| (a) print/raise/警告文案 vs 控制流 | ✅ | ✅ (保留 L1) |
| (b) 注释/docstring 文档引用 (见 X) | 部分 | ✅ business_model + 路径 + 常量分流 |
| (c) 字面引用常量 (module.CONSTANT) | ⛔ | ✅ const_index 自动校验 |
| (d) 字面引用文件路径 (docs/X.md) | ⛔ | ✅ path_ref 自动校验 |
| (e) 字面引用项目内字段 | ⛔ | 留 V5 (低优先) |

**元变量识别 (v2 commit 6b 新增)**: 跳过 `*` / `?` / `{}` / `<>` / `-N` / `-X` /
`your_project` 等占位符路径.

**EXCLUDE_PATH_PREFIXES (v2 commit 6b 新增)**: `scripts/tests/fixtures/` 是测试
材料 self-contained mini-repos, 由各 test_*.py 用 `--root <fixture_dir>` 单独
扫, 不参与全仓扫.

**allowlist (`scripts/tests/r10_allowlist.yaml`)**:
- `paths`: 占位 + 跨仓 + C 类历史/未来 + E 类用户机器/测试材料
- `constants`: F 类历史佐证字面 (commit 4 / 6a / fixture 内引入)
- `self_ref_files`: 同文件自指允许 (SKILL.md / docs/changelog.md 等)
- `business_model_anchors`: 留空, V4-0.1 落地后所有引用应能命中

### 2.2 fixture 设计 (`scripts/tests/fixtures/r10_consistency_check/`)

```
clean/                        ← 0 violations 断言
  scripts/
    valid_module.py           ← 定义 VALID_CONST
    consumer.py               ← 合法引用
    legitimate_lookalikes.py  ← v2 commit 6b 新增: 元变量/跨仓/glob 不误抓
  docs/
    business_model_v1.md      ← 含 §8 #N20 锚点

dirty/                        ← 4 类违规全抓
  scripts/
    bad_refs.py               ← L9-12 各 1 类违规
  docs/
    foo.md                    ← 触发 anchor 违规
```

**断言**:
- clean fixture: 0 violations (含 legitimate_lookalikes.py 不误抓)
- dirty fixture: 4 类违规 (path_ref / const_ref / business_model_anchor /
  business_model_section) 全抓 + 行号精确 (bad_refs.py L9-12)

### 2.3 修复清单

**audit §5 已找到** (commit 3-6 处理):
- #2 build_baseline.py L106-107 stale C-reference reference → commit 3
- #3 SKILL.md L762 引用已删常量 SUB_MODE_JUDGE_PROMPT → commit 4
- #5 parse_tender.py L847 → V4-0.1 落地后自动解 (风险 5 ack), commit 5 跳过
- #6 templates/README.md 首句微调 → commit 5

**Phase 2 全仓扫新发现**:
- b_mode_extract.py L105 引用已删常量 B_MODE_EXTRACT_PROMPT (commit 6 全仓扫
  发现, audit §5 漏的真违规) → commit 6a 修, 同 SKILL.md L762 修法

### 2.4 不动

- 任何业务代码逻辑 / 任何 production_mode / sub_mode 分流路径
- 测试 fixture 之外的 JSON / DOCX

---

## §3 子任务 + commit 序列

| # | commit hash | message | 动到文件 |
| --- | --- | --- | --- |
| 1 | 67a0ebe | docs(v4-0.1): add business_model_v1.md to resolve 16+2 anchor references | docs/business_model_v1.md 新增 |
| 2 | f053221 | test(v4-0.2): add r10_consistency_check.py + clean/dirty fixtures | scripts/tests/r10_consistency_check.py + fixtures + run_all.py 注册 |
| 3 | cd7c224 | fix(v4-0.2): build_baseline.py L106 stale C-reference reference (audit §5 #2) | scripts/build_baseline.py |
| 4 | 37b4025 | fix(v4-0.2): SKILL.md L762 deleted constant reference (audit §5 #3) | SKILL.md L762 |
| 5 | b81063b | fix(v4-0.2): templates/README.md first sentence reflects current state (audit §5 #6) | templates/README.md |
| 6a | 93b70f6 | fix(v4-0.2): commit 6a · 1 真违规 + R10 扫描脚本质量 3 项 | scripts/b_mode_extract.py:105 + scripts/tests/r10_consistency_check.py (emoji crash / exit code / docstring 误报) |
| 6b | 9d53d70 | fix(v4-0.2): commit 6b · 扫描规则升级 + allowlist 扩 + 误报率盲区 fixture | scripts/tests/r10_consistency_check.py (元变量识别 + EXCLUDE_PATH_PREFIXES + 教训 footer) + r10_allowlist.yaml (C/E/F 三类) + test_r10_consistency_check.py (case_8) + clean/legitimate_lookalikes.py |
| 7 | (本 commit) | docs(v4-0): plans/v4-0.md + changelog 总账 | plans/v4-0.md 新增 + docs/changelog.md V4-0 entry |

---

## §4 完成判定

| 判定项 | 验证方式 | 结果 |
| --- | --- | --- |
| docs/business_model_v1.md 存在 | `ls docs/business_model_v1.md` | ✅ commit 67a0ebe |
| 16 处显式 + 2 处隐含 business_model 引用全部实指 | `grep -rn business_model scripts/ SKILL.md` 逐条对照 | ✅ business_model_v1.md 内 §8.3 #N16 / §8 #N19 / #N20 / #N21 / #N22 / #N24 / §9 全实指 |
| r10_consistency_check.py 入仓 + clean fixture pass + dirty fixture 抓全 | `.venv/Scripts/python.exe scripts/tests/r10_consistency_check.py` | ✅ EXIT=0 |
| run_all.py 注册新测试 | `.venv/Scripts/python.exe scripts/tests/run_all.py` | ✅ 13/13 PASS |
| audit §5 #2 #3 #5 #6 全部修 | 逐条对照 | ✅ #2 commit 3 / #3 commit 4 / #5 V4-0.1 自动解 / #6 commit 5 |
| Phase 2 全仓扫新违规全部修 | r10 report 空 | ✅ b_mode_extract.py:105 commit 6a 修 |
| 测试覆盖 ≥ 1 clean + ≥ 1 dirty + 误报率盲区维 | 见 case 1-8 | ✅ 8 case |

---

## §5 风险点 (Phase 1 ack 后裁定结果)

| 风险 | 选项 A | 选项 B | 裁定 |
| --- | --- | --- | --- |
| 1 章节号自洽性 | 只写实际命中的 6 个 #NXX / 跳号 #N17/#N18/#N23 留白 | 推测跳号语义 | **A** (Hugin ack: 字面回溯不补造) |
| 2 scan script 误报 | allowlist self_ref_files 放行同文件自指 | — | **ack** |
| 3 v3 changelog "已对账" 跟事实矛盾 | 不动 changelog 历史字面 / V4-0 新 entry 自带追溯说明 | 在 v3.0.1/v3.0.2 entry 加追溯标注 | **A** |
| 4 templates/README.md 严重度 | 只微调首句 / 不动主体 | — | **ack** |
| 5 parse_tender L847 文案半真半假 | V4-0.1 后自动解 / commit 5 缩为只动 templates/README.md | — | **ack** |
| 6 commit 6 大量违规 | 同型 ≤5 处直接修 / 大量停下报告 | — | **B** (Hugin 裁定: 真违规 1 处 + 65 误报分类处理 / 不塞 allowlist 掩盖能用规则识别的) |

### Phase 2 新发现 (commit 6a + 6b)

- **b_mode_extract.py L105 真违规**: audit §5 漏的, 是 V4-0.2 扫描升级的价值
  实证 (同 SKILL.md L762 修法, 改为引用 business_model §8 #N21)
- **65 处误报二分**: B/C 类 (规范命名占位 + 跨仓/历史/未来引用) → 扩扫描脚本
  规则 (元变量识别) / D/E/F 类 (脚本自身 docstring + fixture 材料 + 历史佐证
  字面) → allowlist 显式登记
- **扫描脚本质量 3 项 (commit 6a)**: emoji Windows GBK crash → 改 ASCII / exit
  code 被 print 副作用污染 → 单独 try/wrap stdout / 自身 docstring 4 处误报
  → 加 EXCLUDE_FILENAMES + 自指 allowlist

### 教训汇总 (进 handbook)

1. **R10 扫描精度演化 (v1→v2)**: 扫描类工具的 fixture 不能只验"抓得全真
   违规" / 还要验"不误抓合法样本" (元变量 / 自指 / 跨仓引用). 这是
   fixture-first clean+dirty 配对的一个盲区维度. (本 commit 加 case_8 +
   legitimate_lookalikes.py + _r10_report.md footer 留痕)
2. **audit 漏发现一例**: b_mode_extract.py L105 引用已删常量, 跟 SKILL.md
   L762 完全同型 — audit 只抓到 L762 漏了 L105, 反证 V4-0.2 扫描升级价值.

---

## audit 计数差异说明

audit-2026-05-27.md (在 handbook/_workspace/ gitignored) 写"14 处" business_model
引用, Phase 0 实测 16 处显式 + 2 处隐含 = 18 处. 本仓 plan / changelog 用 18
口径 (源于实测). audit 文件计数留待 handbook L12/L1 升级 PR 时一起改, 不在
本仓 V4-0 范围动.
