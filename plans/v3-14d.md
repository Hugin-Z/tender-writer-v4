# V3-14d 执行计划 · 开源所需新文件(CONTRIBUTING.md + README 升级)

> 本计划是 V3-14 第四层(d)在 Plan-Execute-Review 协议下的 Phase 1 产出。审核通过后进 Phase 2 自主 commit。
>
> V3-14d 是 1-2h 文档活,新建 1 个 CONTRIBUTING.md + README 升级 2 段(系统依赖 + 项目状态)。LICENSE 已就位(MIT,V3-14a 实测确认),不动。
>
> Phase 0 全部命令真跑贴 plan §0(对齐 feedback_phase0_paste_command_output 强化版)。

---

## 0. Phase 0 关键发现(实测命令输出贴 plan)

### 0.1 README 当前状态

```bash
$ wc -l README.md
95 README.md

$ grep -nE "^## " README.md
11:## 快速开始
29:## 核心能力
42:## 版本演进
61:## 与 AI 工具的兼容性
69:## 典型应用场景
77:## 深度文档
93:## 许可证

$ grep -cE "系统依赖|requirements|Python.*版本" README.md
0  # 缺系统依赖段

$ grep -cE "项目状态|Status|🚧|alpha|beta|稳定" README.md
0  # 缺项目状态段
```

7 个一级标题, **缺 2 段(系统依赖 + 项目状态)**, V3-14d 要补的就是这 2 段。

### 0.2 CONTRIBUTING.md 内容来源体量

```bash
$ wc -l CLAUDE.md references/ai_output_rules.md
  164 CLAUDE.md           # 红线 1-6
  206 references/ai_output_rules.md  # R1-R10
  370 total

$ grep -nE "^### 红线" CLAUDE.md
11:### 红线 1:AI 不得代建用户闸门文件
34:### 红线 2:工具链报错不得标注为"已知 bug"并继续
58:### 红线 3:AI 输出的事实必须有来源
83:### 红线 4:工具链内部术语不外泄到用户产物
104:### 红线 5:AI 不扩展用户字面输入
120:### 红线 6:AI 不得代替用户选择投标主体

$ grep -nE "^## R[0-9]+" references/ai_output_rules.md | head -10
13:## R1 不确定的事实留空,不脑补
32:## R2 用户字面输入不演绎
49:## R3 中文排版无空格(硬约束)
80:## R4 用户约束精确理解,不扩展
97:## R5 tender_brief.md 生成规范
115:## R6 生产模式判断不越界
130:## R7 章节标题关键词的合规性
140:## R8 外部可见性(工具链内部术语不外泄)
163:## R9 不代建用户闸门
177:## R10 工具链报错处置规范
```

### 0.3 Plan-Execute-Review 协议位置(纠正 plan 框架)

用户原 plan 描述说"协议提炼基于 V3 期间 Plan-Execute-Review",实测协议主体在 **`docs/v3_planning.md` L347-383**:

```bash
$ grep -nE "Phase [0-9]|Plan-Execute-Review" docs/v3_planning.md
347:### 审核节奏(Plan-Execute-Review 三阶段协议)
351:**Phase 1: Plan(强制审核点 1)**
359:- 审核位审计划,通过后 Claude Code 进 Phase 2
361:**Phase 2: Execute(无审核,自主执行)**
372:**Phase 3: Review(强制审核点 2)**
379:- 不通过则定位到具体 commit 单独 revert,修复后重走 Phase 3
382:- 启动:plans/v3-N.md 必须经审核位审过才进 Phase 2
383:- 收尾:状态改"已完成"的 commit 必须 Phase 3 审过后才执行
```

**CLAUDE.md 不含协议**(只含 6 条红线), CONTRIBUTING.md 引用协议时用 v3_planning.md 文件级链接(不带行号锚点,文档结构稳定但行号会随后续编辑漂移)。

### 0.4 仓库体量(用于"项目状态"段写实)

```bash
$ git log --oneline | wc -l
97   # commit 总数

$ git ls-files | wc -l
256  # tracked 文件

$ find scripts -maxdepth 1 -name "*.py" | wc -l
33   # 生产代码脚本

$ find scripts/tests -name "test_*.py" | wc -l
12   # 测试脚本
```

**测试 case 数**(从 V3-14b §3.3 / `tmp/audit_b_report.md` 引):

- run_all.py 12 文件全 PASS
- cases += 1 真测得 94 case(test_budget_parsing/test_export_deliverables/test_v45_merge 用别命名风格未计数,实际 case 略多)
- 主指标: 12/12 文件 PASS(稳定值)

### 0.5 V3 期间 commit type 前缀分布(用于 PR 规范设计)

```bash
$ git log --oneline | grep -oE " [a-z]+\(" | awk '{print $1}' | sort | uniq -c | sort -rn
     58 docs(
     19 feat(
     12 test(
      3 fix(
      1 chore(
```

总 93 个 conventional commit 前缀 / 97 总 commit = **96% 遵循 `<type>(scope): subject` 格式**。CONTRIBUTING.md 推荐此 commit 规范有现成例证。

### 0.6 当前依赖(用于"系统依赖"段)

```bash
$ cat requirements.txt
# tender-writer skill 依赖清单
pdfplumber>=0.10.0              # PDF 文本与表格解析
python-docx>=1.1.0              # docx 文档读写
opencc-python-reimplemented>=0.1.7   # 繁简中文转换兜底
lxml>=4.9.0                     # python-docx 底层 XML
pypinyin>=0.50.0                # 中文公司名转拼音
docxtpl>=0.16                   # Word docx 模板填充
PyYAML>=6.0                     # yaml 读写
docxcompose>=1.4                # docx 文件合并(V45)

$ .venv/Scripts/python.exe --version
Python 3.11.9
```

**Python 范围**: requirements.txt 无显式 Python 版本约束。实测 3.11.9 可跑,SKILL.md 文档说 ≥ 3.10。

### 0.7 V3-14a/b/c 审计结果(项目当前健康度依据)

实测 audit_report 系列结果(支撑"项目状态: 稳定可用"判定):

- V3-14a 机器扫: 真敏感命中 1 处(已处理),误报 23 处,0 git tracked 真敏感残留
- V3-14b 行为验证: 13 V3-N 完成判定 12/12 PASS,demo 端到端 11 步全跑通,21 unique 脚本全验证
- V3-14c 语义一致性: 批次 1-3 共 7 commit 修完,plan vs 实现 100% 状态对齐

→ 项目状态**稳定可用**有客观依据(不是空声明)。

---

## 1. 范围与完成判定

### 1.1 范围

**做**(沿用 v3_planning.md V3-14d 范围):

- 新建 `CONTRIBUTING.md`(仓库根),6 章结构(协作模式 / issue 规范 / PR 规范 / 测试 / 文档同步 / 红线引用)
- README.md 加 2 段:**系统依赖**(快速开始之后,核心能力之前) + **项目状态**(badge 之下,核心理念引言之前)
- LICENSE **不动**(已 MIT,Phase 0 §V3-14a 实测确认)

**不做**(留 V4):

- `.github/ISSUE_TEMPLATE` / `.github/PULL_REQUEST_TEMPLATE.md`
- `CODE_OF_CONDUCT.md` / `SECURITY.md`
- README badge 进度标记(D3 决策点,本期建议不加)
- 修改 SKILL.md / CLAUDE.md / docs/* 任何内容(本期范围内只动 README + 新增 CONTRIBUTING)

### 1.2 完成判定

| # | 完成判定 | 自检方式 |
|---|---|---|
| C1 | CONTRIBUTING.md 存在,80-150 行内,含 6 章节 | wc -l + grep 一级标题数 ≥ 6 |
| C2 | CONTRIBUTING.md 引用 v3_planning.md 协议 + CLAUDE.md 红线 + ai_output_rules R1-R10 | grep "v3_planning.md\|CLAUDE.md\|ai_output_rules" CONTRIBUTING.md |
| C3 | README.md 加系统依赖段(grep "系统依赖" 命中 ≥1) | grep -c "系统依赖" README.md ≥ 1 |
| C4 | README.md 加项目状态段(grep "项目状态" 命中 ≥1)+ 含成熟度标签(稳定/alpha/beta) | grep + 检查标签 |
| C5 | README.md 新增段不超 20 行(系统依赖 ≤ 10 行 + 项目状态 ≤ 8 行 + 标题/分隔符)| wc -l README.md 改前 95 → 改后 ≤ 115 |
| C6 | 不动 LICENSE / SKILL / CLAUDE / docs/* | git diff --stat 改动文件清单仅 README.md + CONTRIBUTING.md |

---

## 2. CONTRIBUTING.md 设计

### 2.1 章节结构(6 章, 目标 ~120 行)

```markdown
# 贡献指南

> 欢迎参与 tender-writer 改进。本文档基于 V3 开发期间形成的
> Plan-Execute-Review 三阶段协议提炼,适用于本仓库的代码 / 文档
> / 测试贡献。

## 1. 协作协议

本仓库采用 Plan-Execute-Review 三阶段协议(详见
[docs/v3_planning.md](docs/v3_planning.md)):

- **Phase 1: Plan** — 写 plan 到 `plans/v3-N.md`,审核位审计划,通过后进 Phase 2
- **Phase 2: Execute** — 自主在 main 上线性 commit,自检通过即进下一个
- **Phase 3: Review** — 完工后贴 git log + diff + 自检报告给审核位

每个 V3-N(或未来 V4-N)项严格按此走,中途不允许跳阶段。

## 2. 提 issue

- 用前缀分类: `[BUG]` / `[FEATURE]` / `[DOCS]` / `[QUESTION]`
- bug 报告必含:
  - 复现步骤(最少示例)
  - 期望行为 vs 实际行为
  - Python 版本 / OS / 工具链 commit hash
- feature 请求建议先发 issue 讨论,确认范围后再写 plan

## 3. 提 PR

### 3.1 起 branch

通用规则:

- bug 修复: `fix/<short-desc>`(如 `fix/parse-tender-encoding`)
- 新功能: `feat/<short-desc>`(如 `feat/mcp-assets-provider`)
- 文档: `docs/<short-desc>`

内部维护者按 V-N 计划迭代时(如 V4-1),可用 `feat/v4-1-<desc>` 这种延续 V3 命名,但不强制。

### 3.2 commit message 格式(采用 conventional commits)

`<type>(scope): subject`(V3 期间 96% commit 已采用此格式):

- type 取值: `feat` / `fix` / `docs` / `test` / `refactor` / `chore`
- scope 取值: `v4-N` / 模块名(如 `parse_tender`) / 不带 scope 也可接受
- subject 用动词起头,英文 / 中文均可

例:
- `feat(v4-1): add MCP external assets provider`
- `fix(parse_tender): handle encrypted PDF gracefully`
- `docs(v4-2): update README install section`

### 3.3 PR 描述

- 头部链接对应 plan 文件(如 `plans/v4-N.md`)
- 列 commit 序列与对应文件
- 自检结果(测试 PASS / 烟测命令 / etc)
- 关闭哪个 issue(如有)

## 4. 测试

任何代码改动**必须**附带测试或回归验证:

```bash
./run_script.bat tests/run_all.py
```

合并前 run_all.py **12 个测试文件全 PASS** 是硬要求。新增功能建议加新 test
文件;bug 修复建议加回归 case。fixture 优先合成,不优先拷真实文件
(避免 git 仓体积累积膨胀)。

## 5. 文档同步

任何代码改动伴随相应文档改动:

- 用户可见行为变化 → 改 README / SKILL.md / FAQ.md
- schema 变化 → 改 docs/manifest_schema.md
- 设计决策变化 → 改 docs/DESIGN.md
- 完成判定 / commit 序列 → 改 plans/v3-N.md(或 v4-N.md)

文档语言用中文(项目主开发语境),代码注释 / commit message 中英均可。

## 6. 红线引用

本仓库 6 条硬红线 + 10 条 AI 输出规则,贡献者(尤其 AI 代理)工作前必读:

- [CLAUDE.md](CLAUDE.md): 6 条硬红线(代建闸门 / 报错处置 / 事实来源 /
  内部术语 / 字面输入 / 投标主体选择)
- [references/ai_output_rules.md](references/ai_output_rules.md):
  R1-R10 输出规则(不脑补 / 不演绎 / 中文无空格 / 不扩展 /
  brief 规范 / 模式判别 / 标题关键词 / 外部可见性 /
  不代建闸门 / 报错处置)

PR review 时会重点检查红线遵守情况;违反红线需修正后才能合并。

---

## 协作工具

- 主开发环境: VSCode + Claude Code(本仓库 SKILL.md / CLAUDE.md 为
  Claude Code 优化)
- 兼容 Cline / Cursor / 通义灵码等 AI IDE 插件(只要能读 SKILL.md)
- 测试 / 检查脚本统一通过 `./run_script.bat <脚本名>` 调用,
  不要直接 `python xxx.py`

## 项目维护者

仓库归属: [@Hugin-Z](https://github.com/Hugin-Z)
```

### 2.2 设计要点

- **基于 V3 真实流程提炼**: Phase 1/2/3 + commit 96% conventional 都是实测数据
- **引用而非复制**: CLAUDE.md / ai_output_rules.md / v3_planning.md 不抄全文,链接过去
- **文档语言用中文**: 与现有 SKILL.md / README.md / FAQ.md 一致
- **R8 外部可见性兼容**: 不写 `production_mode / part_attribution / 取档策略 / 素材钩子` 等内部术语
- **commit 规范"采用 conventional commits"措辞**: V3 实测 96% 遵循,留"不带 scope 也可接受"弹性条款
- **测试是硬要求**: run_all.py 全 PASS 不能让

### 2.3 不写的内容(避免 V4 范围漂移)

- 不写"用户作为审核位"概念解释(D4 决策, 我倾向不写, 理由见 §8)
- 不写贡献者协议(CLA)/版权转让(本期开源是个人维护, 不需要 CLA)
- 不写发版流程(V3-14 收尾就 push v3.0.0, 后续 V4 才需要发版规范)
- 不写 GitHub Actions / CI / lint 等(本期无 CI, 留 V4)

---

## 3. README 系统依赖段设计

### 3.1 加在哪里

加在**快速开始之后,核心能力之前**(L26 / L28 之间)。

理由:快速开始是"最快路径",系统依赖是"细节读者补充信息"。先快速后详细的阅读流。

### 3.2 内容(目标 ~10 行)

```markdown
## 系统依赖

- **Python**: 3.11+(实测版本;3.10 未做兼容性验证)
- **OS**: Windows 10+(主开发环境);Linux / macOS 未在 CI 验证,
  社区贡献欢迎
- **git**: 必装(`scripts/demo_reset.py` 等运维工具依赖 git CLI)
- **Python 包**: 8 个核心包(pdfplumber / python-docx / docxtpl / PyYAML
  / docxcompose / opencc / lxml / pypinyin),完整清单 [requirements.txt](requirements.txt)
- **可选**: OCR 工具(扫描版 PDF 处理时按需,详见 [docs/FAQ.md Q4](docs/FAQ.md))

依赖通过 `install.bat` 自动装到隔离虚拟环境(`.venv/`),不污染系统 Python。
```

### 3.3 不重复 install.bat 内容

只列"用户决策需要的信息"(版本范围 / OS 兼容性 / 是否要装 git),不列具体 pip install 步骤(install.bat 自动处理)。

---

## 4. README 项目状态段设计

### 4.1 加在哪里

加在**badge 之下、核心引言之前**(L4 / L5 之间)。

理由: 用户打开 README 第一眼看到"项目稳定/不稳定",再决定要不要继续读。

### 4.2 内容(目标 ~8 行)

```markdown
## 项目状态

**稳定可用(主开发环境)** — v3.0.0 已封板,完成 14 项改进(V3-1 至 V3-14)。Linux / macOS 未验证。

- **测试覆盖**: 12 个测试文件 / `./run_script.bat tests/run_all.py` 全 PASS
- **demo 项目**: `projects/demo_cadre_training/` 端到端可重跑(parse_tender → ... → export_deliverables 11 步)
- **commit 总数**: 97 个,96% 遵循 `<type>(scope): subject` 规范
- **主开发环境**: Windows 10+ + Python 3.11,Linux/macOS 未验证
```

### 4.3 不夸大

- 不写"production-ready"(目前是单人项目 + 1 个 demo + 主开发 Windows,production-ready 太满)
- 不写"100% 测试覆盖"(实际 case 覆盖核心模块,边界覆盖率不全)
- 不写"高性能"(没做 benchmark)
- 写"稳定可用"是对应 V3-14a/b/c 审计结果(0 真敏感残留 / 12 V3-N 回归全过 / 一致性审完)的客观描述

### 4.4 D0 决策点 — 成熟度标签

| 标签 | 适用场景 | 我的判断 |
|---|---|---|
| `alpha` | 实验性,API 不稳定 | ❌ V3 已封板, 不 alpha |
| `beta` | 主功能可用,可能有边界 bug | ⚠️ 比较保险,但 V3-14b 12/12 PASS 已超 beta |
| **`稳定可用`(stable)** | **主功能稳定, 已端到端跑通** | ✅ **推荐** |
| `production-ready` | 多用户验证, 多平台跑过, 有 SLA | ❌ 单人项目,不到 |

**建议 D0 = `稳定可用`(stable)**, 客观对应 V3-14 审计结果。

---

## 5. Commit 序列(Phase 2 自主执行)

### 5.1 推荐方案: 2 commit(D2 决策点)

| # | message | 动到的文件 |
|---|---|---|
| 1 | `feat(v3-14d): add CONTRIBUTING.md` | `CONTRIBUTING.md`(新)|
| 2 | `docs(v3-14d): add README system requirements + project status sections` | `README.md`(2 段新增) |

理由:
- CONTRIBUTING.md 是新文件,独立 commit 利于回滚
- README 2 段同一文件改动,合并 1 commit 更紧凑
- 共 2 commit,较 3 commit 方案少 1 个,符合"短任务 commit 数收窄"经验

### 5.2 备选方案: 3 commit(细粒度)

如果用户偏好细粒度:
1. `feat(v3-14d): add CONTRIBUTING.md`
2. `docs(v3-14d): add system requirements section to README`
3. `docs(v3-14d): add project status section to README`

最后 + 1 收尾 commit `docs(v3-14): mark completed`(整 V3-14 收尾,不属 14d 子范围)。

---

## 6. 自检方式(Phase 3 完工时执行)

### 6.1 字面验证

```bash
# C1: CONTRIBUTING 存在 + 行数范围
wc -l CONTRIBUTING.md
# 期望: 80-150 行

# C1: 6 章节
grep -cE "^## [0-9]+\." CONTRIBUTING.md
# 期望: ≥ 6(章节标题数)

# C2: 引用 3 个来源
grep -cE "v3_planning\.md|CLAUDE\.md|ai_output_rules" CONTRIBUTING.md
# 期望: ≥ 3

# C3 + C4: README 新增 2 段
grep -c "系统依赖" README.md
# 期望: ≥ 1
grep -cE "项目状态.*稳定可用" README.md
# 期望: ≥ 1

# C5: README 行数控制
wc -l README.md
# 期望: ≤ 115(改前 95 + 新增 ≤ 20 行)

# C6: git diff 范围
git diff --stat HEAD~2 HEAD --name-only
# 期望: 仅 CONTRIBUTING.md + README.md
```

### 6.2 主观阅读

完工后贴 README 全文 + CONTRIBUTING.md 全文给审核位,主观判定:

- README 阅读流畅性(系统依赖 + 项目状态加入是否破坏原节奏)
- CONTRIBUTING 对开源新人友好度(是否过偏向"内部协作")
- 措辞合规(R8 内部术语不外泄)

---

## 7. 风险

| 风险 | 缓解 |
|---|---|
| CONTRIBUTING.md 措辞过偏向"内部协作"(Plan-Execute-Review)对开源新人不友好 | §6 自检阅读流畅性,Phase 3 让用户判;协议是真实的就保留,改写只保留接口而非简化协议本身 |
| README 项目状态写"稳定可用"但实际只 1 个 demo,过度承诺 | "稳定可用"对应 V3-14a/b/c 审计结果,不是空声明;明示"主开发 Windows / Linux+macOS 未验证"留余地 |
| 系统依赖段与 install.bat 内容重复 | 系统依赖只列"决策信息"(版本范围 / OS / git / 可选 OCR),不写具体 pip install 步骤 |
| README 加 2 段后行变长破坏阅读节奏 | 新增 ≤ 20 行(C5 约束),95→ ≤ 115,增量小 |
| CONTRIBUTING 引用 docs/v3_planning.md 锚点行号会失效 | 已落实:用 v3_planning.md 文件级链接,无锚点 |
| commit 规范措辞引发歧义 | 已落实:措辞用"采用 conventional commits 格式",保留"不带 scope 也可接受"弹性条款,V3 期间 96% 遵循实测数据为依据 |

---

## 8. 待用户审核点(已审定)

- **D0(已采纳)**: README 项目状态成熟度标签 = `稳定可用(主开发环境)`,Linux/macOS 未验证限定明示
- **D1(已采纳)**: CONTRIBUTING.md commit 规范用"**采用 conventional commits**"措辞(V3 期间 96% 遵循实测数据),保留"不带 scope 也可接受"弹性条款
- **D2(已采纳)**: commit 序列 = 2 commit(CONTRIBUTING + README 升级各 1)
- **D3(已采纳)**: 不加"V3 14/14"badge(V4 启动后过时,留 V4 配 dynamic badge)
- **D4(已采纳)**: CONTRIBUTING.md 不提"用户作为审核位"概念(开发期临时角色,开源后泛化)
- **D5(已采纳)**: CONTRIBUTING.md 不加 CLA(单人维护无需,留 V4 / 首个外部 PR 时按需加)

审核已通过 + 7 处 fix 修订落地,进 Phase 2 自主 2 个 commit + 完工 Phase 3 审核(贴 git diff + 自检 + README/CONTRIBUTING 全文)。
