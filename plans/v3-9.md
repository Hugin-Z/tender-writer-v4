# V3-9 执行计划 · check_cross_consistency 扩充检查项

> 本计划是 V3-9 在 Plan-Execute-Review 协议下的 Phase 1 产出。审核通过后进 Phase 2 自主 commit。
>
> V3-9 是 4-6h 中等活，扩充 3 项检查（项 4/5/6）。**Plan 阶段已用 .venv python 拆 demo 真实数据**（对齐 feedback_fixture_verify "不要纸上推算"教训），关键发现：demo 自身就有项目名 brief vs docx 不一致问题，项 4 在 demo 上能真实触发；项 5/6 demo 数据稀疏，必须合成 fixture。

---

## 0. Phase 0 关键发现

### 0.1 demo 真实数据实测

拆 `projects/demo_cadre_training/output/tender_brief.json` + `final_tender_package/final_response.docx`：

| 字段 / 维度 | brief.json (extracted) | docx 实际出现 | 差异 |
|---|---|---|---|
| project_name | `A 直属单位 2026 年度干部综合**素质**能力培训服务采购项目` | `A **市**直属单位 2026 年度干部综合能力**提升**培训服务采购项目`（出现 3 次，全部一致版本）| **brief vs docx 不一致**（多 1 字"市" + "素质" vs "提升"）|
| project_number | `DEMO-2026-001` | `DEMO-2026-001`（出现 4 次） | 一致 |
| buyer_name | `A 中央党校干部学院` | docx 0 次出现完整名，全部用代称"采购人" | docx 用代称 |
| buyer_agency_name | `示例咨询代理有限公司` | docx 0 次出现 | docx 不出现 |
| 简历 / 组织架构 人名 | N/A | 用占位符 `【待填:项目负责人姓名】` + 描述（"省部级培训中心认证讲师"）| **demo 没有典型人名样本** |
| 章节交叉引用 | N/A | docx 完整 Heading 1/2/3 体系（一/二/三/四/五 + X.X + X.X.X 共 ~25 章节），但全文 **0 处** "详见 X.Y" 引用 | **demo 没有典型引用样本** |

**结论**：

1. **项 4 在 demo 上能真实触发**——brief vs docx 项目名不一致是天然 dirty case，项 4 落地即可验证算法
2. **项 5 / 项 6 demo 无样本**——必须合成 fixture（clean / dirty 配对）
3. **采购方多以代称出现**——项 4 算法要允许"代称模式"（不强求 buyer_name 字面出现）

### 0.2 既有代码基线

`scripts/check_cross_consistency.py` 当前结构（V2 + v2.0.1 团队上下文）：

- L29-44：数字抽取常量（`_MONEY_RE` / `_DAYS_RE` / `_PERSON_RE` / `_D_PLUS_RE` / `_TEAM_CONTEXT_WORDS`）
- L46-56：`extract_docx_text()` 拼段落 + 表格单元格成纯文本
- L59-98：parse_* 系列（金额 / 天数 / D+N / 人数）
- L105-207：3 项现有检查函数（`check_duration_vs_dplus` / `check_team_cost_vs_budget` / `check_key_numbers_consistency`）
- L214-326：main + _main_body（CLI + brief 加载 + 三项检查调用 + 汇总）

`scripts/tests/test_check_cross_consistency.py` 当前 12 case，全部 inline string fixture（无 docx fixture）。

V3-9 沿用既有"[失败] / [警告] / [通过] / [信息]"四档报告 + 退出码（≥1 失败 → exit 1）。

**关键实测（调整 5 已验证）**：`extract_docx_text` 函数源码确认含 `for cell in row.cells: chunks.append(cell.text)` —— **段落 + 表格 cell 全部拼接成纯文本**。因此项 6 全文扫引用编号自动覆盖表格 cell 内容，无需为表格单独加扫描分支。fixture `dirty_section_ref.docx` 加一处**表格内**错引用反例验证此覆盖。

### 0.3 V3-6 fallback 模式兼容性声明（feedback_fallback_downstream_compat）

V3-9 三项新检查的输入：

| 检查项 | 输入字段 | 是否依赖 brief.tables |
|---|---|---|
| 项 4 项目名 / 采购方一致性 | `extracted.project_name / buyer_name / buyer_agency_name` + docx 全文 | **否** |
| 项 5 简历 / 架构人名一致性 | docx 章节切分（Heading 1/2/3） | **否** |
| 项 6 章节交叉引用 | docx 章节锚点 + 全文 | **否** |

**结论：V3-9 三项新检查 0 处使用 `tables` 字段**，V3-6 fallback 模式（`--ocr-text` 跳过 PDF 文字提取，tables 为空）下 V3-9 行为不受影响。Plan 内显式记录此事实，避免 V3-9 落地后未来 V3-N+ 误改 tables 依赖时疏漏。

---

## 1. 范围与完成判定

### 1.1 范围

**做**：

- 在 `check_cross_consistency.py` 加 3 个新 check 函数（项 4/5/6）
- main 集成 3 项调用 + 汇总输出
- 新建 `scripts/tests/fixtures/check_cross_consistency/` 目录 + `_generators/make_test_docx.py` + 4 个 docx fixture + 1 个 brief_minimal.json fixture
- `test_check_cross_consistency.py` 增量加 ≥6 个 case（每项 ≥1 正例 + ≥1 反例，并含跨人 NAME_RE 边界 / 表格 cell 引用边界）
- 不动既有 V2 / V3-2 代码或测试（项 1/2/3 + parse_* 函数 + case 1-12 全部不动）
- 不动 `requirements.txt`

**不做**：

- 不改 brief.json schema（输入字段全部用既有 `extracted.*`）
- 不引入新依赖（用 stdlib re + python-docx 已有）
- 不做"语义级"NLP 比对（项目名 normalize 走 token-level 字符比，不引入 OpenCC/jieba 之外的库）
- 不改 V3-6 fallback 路径（V3-9 三项检查与 tables 解耦，无需 fallback 适配）

### 1.2 完成判定

| # | 完成判定 | 自检方式 |
|---|---|---|
| C1 | 项 4 在 demo 上真实触发，输出"brief vs docx 项目名不一致"警告 | demo 烟测：跑 check_cross_consistency 看 [项 4] 段落 |
| C2 | 项 4 采购方代称模式不误报 | demo 烟测 buyer_name 走代称路径 → 不报 fail |
| C3 | 项 5 fixture 上 clean 通过 / dirty（人名职称冲突）触发 [失败] | test fixture clean_response.docx + dirty_resume_org.docx |
| C4 | 项 6 fixture 上 clean 通过 / dirty（章节引用编号不存在，含表格 cell 内错引用）触发 [失败] | test fixture dirty_section_ref.docx |
| C5 | V3-2 既有 case 1-12 全部 PASS（不破坏既有功能）| 跑 test_check_cross_consistency.py，commit 1 落地后立即跑一次,任何 case fail 在 commit 1 内修复 |
| C6 | V3-6 fallback 模式与 V3-9 解耦：tables 字段在 V3-9 任一检查中均未引用 | grep tables 关键词在 V3-9 加的 3 个函数中 0 命中 |
| C7 | NAME_RE 不跨人误抓（"张三...李四 担任技术负责人" 不抓张三+技术负责人）| commit 1 落地后立即用 fixture 内同段两人名场景验证 |

---

## 2. 实现设计

### 2.1 项 4：项目名 / 采购方 / 代理一致性

**函数签名**：

```python
def check_field_consistency(
    extracted: dict,
    docx_text: str,
    docx_headings: list[str] | None = None,
) -> list[str]:
    """V3-9 项 4: 检查 brief vs docx 字段一致性,允许代称模式。

    - project_name: 优先扫 docx 前 500 字符 + Heading 1 标题(项目名最常见在封面/章节标题)
                    若上述均无 jaccard ≥ 0.4 候选,降级全文"项目"附近 30 字窗口
    - buyer_name / buyer_agency_name: docx 完整出现 → 通过;0 次 + 含代称 → 信息;0 次 + 无代称 → 警告
    """
```

**字段 normalize 算法**（核心）：

```python
def _normalize_for_token(s: str) -> list[str]:
    """把字段切成 4 字滑窗 token 集合(用于 jaccard 相似度)。"""
    s = re.sub(r'\s+', '', s)
    s = re.sub(r'[，。、（）()【】《》""''""]', '', s)
    if len(s) < 4:
        return [s]
    return [s[i:i+4] for i in range(len(s) - 3)]


def _jaccard(a: list[str], b: list[str]) -> float:
    sa, sb = set(a), set(b)
    if not sa or not sb:
        return 0.0
    return len(sa & sb) / len(sa | sb)
```

**项 4 主体（含调整 4 候选窗口策略）**：

```python
def check_field_consistency(
    extracted: dict, docx_text: str, docx_headings: list[str] | None = None,
) -> list[str]:
    issues = []
    pn = (extracted.get('project_name') or '').strip()
    bn = (extracted.get('buyer_name') or '').strip()
    ba = (extracted.get('buyer_agency_name') or '').strip()

    if pn:
        pn_tokens = _normalize_for_token(pn)
        # 候选窗口 1: docx 前 500 字符(封面区,项目名最常出现)
        # 候选窗口 2: 每个 Heading 1 标题(章节标题常含项目名变体)
        candidates = [docx_text[:500]]
        if docx_headings:
            candidates.extend(docx_headings)

        best_jaccard = max(
            (_jaccard(pn_tokens, _normalize_for_token(c)) for c in candidates),
            default=0.0,
        )

        # 降级:前面候选都 < 0.4,扫全文"项目"附近 30 字窗口
        if best_jaccard < 0.4:
            fallback_cands = []
            for m in re.finditer(r'项目', docx_text):
                start = max(0, m.start() - 30)
                end = min(len(docx_text), m.end() + 5)
                fallback_cands.append(docx_text[start:end])
            if fallback_cands:
                fallback_jaccard = max(
                    _jaccard(pn_tokens, _normalize_for_token(c)) for c in fallback_cands
                )
                best_jaccard = max(best_jaccard, fallback_jaccard)
                candidates.extend(fallback_cands)

        # 报告
        if best_jaccard >= 0.7:
            issues.append(f"[通过] 项目名一致性: brief vs docx jaccard={best_jaccard:.2f}")
        elif best_jaccard >= 0.4:
            best = max(candidates, key=lambda c: _jaccard(pn_tokens, _normalize_for_token(c)))
            issues.append(
                f"[警告] 项目名 brief vs docx 部分不一致: jaccard={best_jaccard:.2f}\n"
                f"           brief: {pn!r}\n"
                f"           docx 最近候选: {best.strip()[:100]!r}"
            )
        else:
            issues.append(
                f"[失败] 项目名 brief vs docx 严重不一致: jaccard={best_jaccard:.2f}\n"
                f"           brief: {pn!r}"
            )

    # 采购方 / 代理:代称模式兼容
    for label, val in [('采购方 buyer_name', bn), ('代理 buyer_agency_name', ba)]:
        if not val:
            continue
        normalized_docx = re.sub(r'\s+', '', docx_text)
        normalized_val = re.sub(r'\s+', '', val)
        if normalized_val in normalized_docx:
            issues.append(f"[通过] {label} docx 完整出现")
        else:
            has_proxy_term = any(
                term in docx_text for term in ['采购人', '招标方', '委托方', '甲方']
            )
            if has_proxy_term:
                issues.append(
                    f"[信息] {label}={val!r} docx 用代称(采购人/招标方/委托方/甲方),不强校验"
                )
            else:
                issues.append(f"[警告] {label}={val!r} docx 既无完整名也无代称")

    return issues
```

**关键设计点**：

- **候选窗口分层（调整 4）**：
  - 一档：docx 前 500 字符（封面区）+ 每个 Heading 1 标题
  - 降级：上述都 < 0.4 才走全文"项目"附近 30 字窗口
  - 理由：项目名最常出现在封面/章节标题，全文窗口是兜底兼容
- jaccard 阈值 **0.7 通过 / 0.4-0.7 警告 / < 0.4 失败**（V3-4 阈值演化经验, demo 烟测后 1 档 autonomous 调整, 跨档停下汇报）
- 4 字滑窗：保留语义单元（"直属单位" / "综合能力"），ngram 太短噪声大

### 2.2 项 5：简历 / 组织架构 人名一致性

**函数签名**：

```python
def check_resume_org_consistency(docx_path: Path) -> list[str]:
    """V3-9 项 5: 检查简历章节 vs 组织架构图 中的人名职称对应关系。"""
```

**关键词清单（含调整 1）**：

```python
RESUME_SECTION_KEYWORDS = [
    '简历', '师资', '团队成员', '人员配置', '项目人员', '骨干', '人员',
]
ORG_SECTION_KEYWORDS = [
    '组织架构', '组织结构', '项目组织', '人员架构', '团队架构', '团队组成',
]
```

**职称白名单（含调整 2,共 15 个）**：

```python
NAME_RE = re.compile(
    r'([一-龥]{2,4})\s*(?:[,，:：、]|担任|为|是|出任)?\s*'
    r'(项目经理|项目负责人|技术负责人|主讲|讲师|顾问|专家|总监|副总|经理'
    r'|架构师|工程师|设计师|实施|运维)'
)
```

**算法**：

```python
def _split_sections_by_heading(doc) -> dict[str, list[str]]:
    """按 Heading 1/2/3 切分章节,返回 {章节标题: [段落文本]}。"""
    sections = {}
    cur_title = None
    cur_paras = []
    for p in doc.paragraphs:
        style = getattr(p.style, 'name', '')
        if 'Heading' in style and p.text.strip():
            if cur_title is not None:
                sections[cur_title] = cur_paras
            cur_title = p.text.strip()
            cur_paras = []
        elif p.text.strip():
            cur_paras.append(p.text)
    if cur_title is not None:
        sections[cur_title] = cur_paras
    return sections


def check_resume_org_consistency(docx_path: Path) -> list[str]:
    issues = []
    doc = Document(str(docx_path))
    sections = _split_sections_by_heading(doc)

    resume_sections = {t: paras for t, paras in sections.items()
                       if any(kw in t for kw in RESUME_SECTION_KEYWORDS)}
    org_sections = {t: paras for t, paras in sections.items()
                    if any(kw in t for kw in ORG_SECTION_KEYWORDS)}

    if not resume_sections:
        issues.append("[信息] docx 未识别到简历章节,跳过项 5")
        return issues
    if not org_sections:
        issues.append("[信息] docx 未识别到组织架构章节,跳过项 5")
        return issues

    def extract_pairs(paras: list[str]) -> dict[str, set[str]]:
        d = {}
        for para in paras:
            for m in NAME_RE.finditer(para):
                name, title = m.group(1), m.group(2)
                if any(skip in name for skip in ['待填', 'XX', '示例', '某某']):
                    continue
                d.setdefault(name, set()).add(title)
        return d

    resume_paras = sum(resume_sections.values(), [])
    org_paras = sum(org_sections.values(), [])
    resume_pairs = extract_pairs(resume_paras)
    org_pairs = extract_pairs(org_paras)

    only_in_resume = set(resume_pairs) - set(org_pairs)
    only_in_org = set(org_pairs) - set(resume_pairs)

    for name in only_in_resume:
        issues.append(
            f"[警告] 人员 {name!r} 在简历章节出现但组织架构图未列(职称: {resume_pairs[name]})"
        )
    for name in only_in_org:
        issues.append(
            f"[警告] 人员 {name!r} 在组织架构图出现但简历章节未列(职称: {org_pairs[name]})"
        )

    common = set(resume_pairs) & set(org_pairs)
    for name in common:
        if resume_pairs[name] & org_pairs[name]:
            issues.append(f"[通过] 人员 {name!r} 在简历和架构中职称一致或交集非空")
        else:
            issues.append(
                f"[失败] 人员 {name!r} 在简历(职称 {resume_pairs[name]}) 和架构图"
                f"(职称 {org_pairs[name]}) 中职称完全不一致"
            )

    if not resume_pairs and not org_pairs:
        issues.append("[信息] 简历/架构章节均未抽到具名人员,跳过(可能用占位符 / 描述)")

    return issues
```

**关键设计点**：

- **章节识别用 Heading 标题关键词清单**（调整 1 加 "人员" + "团队组成"）
- **职称白名单 15 个**（调整 2 加架构师/工程师/设计师/实施/运维）
- **占位符过滤**：跳过含"待填" / "XX" / "示例" / "某某" 的"姓名"
- **NAME_RE 跨人风险（C7 调整 6）**：正则隐含约束是"姓名" + 0-N 字 + "职称"，N 大可能误抓"张三...李四 担任技术负责人"。fixture clean_response.docx 内显式构造同段两人名场景，commit 1 落地后立即跑 fixture 验证 NAME_RE 不跨人。如实际跨人误抓，**commit 1 内**收紧 NAME_RE 到"姓名 + 0-10 字内 + 职称"（同 commit 修复，不另开 commit）。
- skip 模式：两类章节缺一就 [信息] 跳过

### 2.3 项 6：章节交叉引用编号正确性

**extract_docx_text 已覆盖表格 cell（调整 5 实测确认）**：

调整 5 实测 `scripts/check_cross_consistency.py` L46-56 `extract_docx_text` 源码,确认含 `for cell in row.cells: chunks.append(cell.text)`——**段落 + 表格 cell 全部拼接成纯文本返回**。因此项 6 用既有 `extract_docx_text(docx_path)` 全文扫描,**自动覆盖表格 cell 内的引用**。无需为表格单独加扫描分支。

fixture `dirty_section_ref.docx` 中**专门加一处表格 cell 内的错引用反例**（如表格中"详见 8.5 节"），验证算法对表格内容的覆盖。

**函数签名**：

```python
def check_section_ref_validity(docx_path: Path) -> list[str]:
    """V3-9 项 6: 检查 docx 内章节交叉引用编号是否对应实际章节(段落 + 表格)。"""
```

**算法**：

```python
REF_RE = re.compile(
    r'(?:详见|见|参见|参考|参照)\s*'
    r'(?:第\s*)?'
    r'(\d+(?:\.\d+){0,2})'
    r'\s*(?:章|节|条|款)?'
)

HEADING_NUM_RE = re.compile(r'^(\d+(?:\.\d+){0,2})')


def check_section_ref_validity(docx_path: Path) -> list[str]:
    issues = []
    doc = Document(str(docx_path))

    valid_nums = set()
    for p in doc.paragraphs:
        style = getattr(p.style, 'name', '')
        if 'Heading' not in style:
            continue
        title = p.text.strip()
        m = HEADING_NUM_RE.match(title)
        if m:
            valid_nums.add(m.group(1))

    if not valid_nums:
        issues.append("[信息] docx 未识别到带编号的 Heading,跳过项 6")
        return issues

    text = extract_docx_text(docx_path)  # 段落 + 表格 cell 全包
    bad_refs = []
    good_count = 0
    for m in REF_RE.finditer(text):
        num = m.group(1)
        end_pos = m.end()
        if end_pos < len(text) and text[end_pos] in '元万亿千百':
            continue
        if num in valid_nums:
            good_count += 1
        else:
            parent = num.rsplit('.', 1)[0] if '.' in num else None
            if parent and parent in valid_nums:
                good_count += 1
            else:
                bad_refs.append((num, m.group(0)))

    if not bad_refs and good_count == 0:
        issues.append("[信息] docx 未识别到典型章节交叉引用,跳过项 6")
        return issues

    if bad_refs:
        for num, raw in bad_refs[:10]:
            issues.append(
                f"[失败] 章节引用 {raw!r} 指向编号 {num!r},但 docx 章节体系不存在该编号"
            )
    if good_count:
        issues.append(f"[通过] {good_count} 处章节交叉引用编号有效")

    return issues
```

**关键设计点**：

- **必须有引用动词**（详见 / 见 / 参见 / 参考 / 参照）
- **排除单位干扰**：编号后跟"元 / 万 / 亿 / 千 / 百" 跳过
- **父级容忍**：引用 1.2.3 但 docx 只有 1.2 → 通过
- **bad_refs 限 10 条**：避免大文档堆叠
- **表格 cell 自动覆盖（调整 5 已验证）**：用既有 `extract_docx_text` 即可

### 2.4 main 集成

`_main_body(args)` 末尾加 3 个新检查调用，沿用既有"[检查 N]"格式：

```python
# Heading 1 标题列表传给项 4(候选窗口)
docx_headings = []
for p in Document(str(docx_path)).paragraphs:
    style = getattr(p.style, 'name', '')
    if style == 'Heading 1' and p.text.strip():
        docx_headings.append(p.text.strip())

print("[检查 4] 字段一致性: 项目名 / 采购方 / 代理")
issues = check_field_consistency(extracted, text, docx_headings=docx_headings)
for msg in issues:
    print(f"  {msg}")
all_issues.extend(issues)
print()

print("[检查 5] 简历 / 组织架构 人名一致性")
issues = check_resume_org_consistency(docx_path)
for msg in issues:
    print(f"  {msg}")
all_issues.extend(issues)
print()

print("[检查 6] 章节交叉引用编号正确性")
issues = check_section_ref_validity(docx_path)
for msg in issues:
    print(f"  {msg}")
all_issues.extend(issues)
print()
```

汇总语句不改（继续 `fail_count + warn_count`，fail ≥ 1 → exit 1）。

---

## 3. 误报控制 + 调整路径（对齐 V3-4 经验）

| 项 | 误报场景 | 控制方式 |
|---|---|---|
| 项 4 | 项目名 docx 没出现完整名（用代称"本项目"）| 候选窗口分层 + jaccard 阈值 0.4-0.7 走警告;阈值 V3 hardcode,demo 烟测误报率 ≥ 50% (3/6 假警告) 触发调整,1 档 0.5/0.7 → autonomous,跨档（< 0.5）停下汇报 |
| 项 4 | 采购方 docx 用代称"采购人" | 代称模式 [信息] 不报失败 |
| 项 5 | 章节标题不含关键词 | 关键词清单 hardcode 7+6（调整 1 已扩容）,demo 烟测发现遗漏关键词补一档 → autonomous |
| 项 5 | NAME_RE 跨人误抓（"张三...李四 担任技术负责人"）| commit 1 落地后立即跑 fixture 同段两人名场景验证（C7）;实际跨人 → commit 1 内收紧"姓名 + 0-10 字内 + 职称"（同 commit 修复）|
| 项 5 | NAME_RE 误抓非人名 | 限制 NAME_RE 后跟职称白名单 15 个（调整 2 扩容） + 占位符过滤 + skip 模式 |
| 项 6 | 误抓"3.1 万元" 等非引用 | 必须有引用动词 + 排除单位字符 |
| 项 6 | 引用 1.2.3 但 docx 只到 1.2（合理细分）| 父级容忍 |
| 全部 | 检测过严,用户实在排不掉 | 不引入 `--skip-check-N` flag（V3 不需要,V4 评估）;实在误报 → 按 V3-4 经验登记 issue + V4 阈值调整 |

阈值在 V3 期间均 hardcode：
- 项 4 jaccard 通过/警告/失败 = 0.7 / 0.4
- 项 5/6 无数值阈值

---

## 4. 测试 fixture 设计

### 4.1 fixture 目录结构

新建 `scripts/tests/fixtures/check_cross_consistency/`：

```
check_cross_consistency/
├── _generators/
│   └── make_test_docx.py
├── README.md                         # fixture 来源说明
├── brief_minimal.json                # 最小 brief 配套 docx fixture
├── clean_response.docx               # 4-5 章节,项目名一致,简历+架构齐备含具名人员,引用全有效,**含同段两人名场景验证 C7**
├── dirty_project_name.docx           # 项目名与 brief 字面不一致(仿 demo 案例)
├── dirty_resume_org.docx             # 同名 张三 简历=项目经理,架构图=技术负责人
└── dirty_section_ref.docx            # 引用"详见 8.5 节" 但 docx 章节最大到 4.x;**含 1 处表格 cell 内错引用**
```

clean_response.docx 内**显式构造同段两人名场景**（调整 6 / C7）：

```
本项目由张三负责整体管理,李四担任技术负责人,王五负责具体实施工作。
```

期望：NAME_RE 抓"李四 + 技术负责人"、"王五 + 实施"，**不**抓"张三 + 技术负责人"。

dirty_section_ref.docx 内**含 1 处表格 cell 错引用**（调整 5 验证表格覆盖）：

```
docx 章节最大到 4.x;
正文段落: "详见 8.5 节"  → 错引用反例 1(段落)
表格 cell: "依据 9.1 节标准执行" → 错引用反例 2(表格 cell,验证 extract_docx_text 覆盖)
```

### 4.2 fixture 提前验证（避免 V3-4 case 6 教训）

写测试 case 之前，用 .venv python 跑（commit 1 落地后立即做）：

- clean_response.docx 跑 3 项检查 → 全部 [通过] 或 [信息]，**项 5 显式确认 NAME_RE 不跨人误抓张三+技术负责人**
- dirty_project_name.docx 跑项 4 → 含 [警告] 或 [失败]
- dirty_resume_org.docx 跑项 5 → 含 [失败]
- dirty_section_ref.docx 跑项 6 → 含 [失败]，**且失败列表覆盖段落引用 + 表格 cell 引用两处**

期望值跑一遍看真实输出再 hardcode 到 case。

### 4.3 测试 case（test_check_cross_consistency.py 增量,V3-2 case 1-12 不动）

| # | fixture | 期望 |
|---|---|---|
| 13 | clean_response.docx + brief_minimal.json | check_field_consistency / resume_org / section_ref 全部 [通过] / [信息]，0 [失败]；含 NAME_RE 同段两人名验证（张三不被误抓技术负责人）|
| 14 | dirty_project_name.docx + brief_minimal.json | check_field_consistency 含 [警告] 或 [失败]（jaccard 不达标）|
| 15 | dirty_resume_org.docx | check_resume_org_consistency 含 [失败]（人名职称冲突）|
| 16 | dirty_section_ref.docx | check_section_ref_validity 含 ≥2 个 [失败]（段落引用 + 表格 cell 引用都被抓）|
| 17 | inline `_normalize_for_token` + `_jaccard` 单元 | 完全相同 → 1.0;完全不同 → 0.0;部分重合 → 0.X |
| 18 | inline NAME_RE / REF_RE 边界 | 占位符 `【待填:姓名】` 不命中 NAME_RE;"3.1 元" 不命中 REF_RE;同段两人名"张三...李四 技术负责人" 只匹配李四 |

### 4.4 demo 烟测（项 4 真实触发验证）

跑 `check_cross_consistency.py projects/demo_cadre_training/final_tender_package/final_response.docx --brief projects/demo_cadre_training/output/tender_brief.json`：

期望：

- [项 4] 报项目名不一致警告或失败（demo 自身天然 dirty case）
- [项 5] 报 [信息] skip（demo 用占位符 + 描述,无具名人员）
- [项 6] 报 [信息] skip（demo 无典型引用）
- 既有 [项 1/2/3] 行为不变

**Phase 3 review 报告里明确说**（调整 8 / D6）：demo 项目名 brief vs docx 不一致是**已知 demo 数据问题**，不是 V3-9 引入的；V3-9 检测算法在 demo 上**正确触发警告**，证明算法工作正确。

---

## 5. Commit 序列（Phase 2 自主执行,2 个 commit,调整 3）

| # | message | 动到的文件 |
|---|---|---|
| 1 | `feat(v3-9): add 3 new cross-consistency checks (items 4/5/6)` | `scripts/check_cross_consistency.py`（3 个 check 函数 + normalize 工具 + main 集成 + Heading 1 抽取）|
| 2 | `test(v3-9): add fixtures + 6 cases for items 4/5/6` | fixture 目录 + 4 个 docx + 1 个 json + generator + test_check_cross_consistency.py case 13-18 |

**commit 1 落地后立即自检（调整 7 + C5 + C7）**：

```bash
# A. import 验证
.venv/Scripts/python.exe -c "import sys; sys.path.insert(0,'scripts'); from check_cross_consistency import check_field_consistency, check_resume_org_consistency, check_section_ref_validity; print('OK')"

# B. V3-2 既有 case 1-12 零回归(C5)
./run_script.bat tests/test_check_cross_consistency.py
# 期望: case 1-12 全 PASS(commit 1 还没加 case 13-18,跑出来就是原来的)
# 任一 case fail → 在 commit 1 内修复, 不另开 commit
```

**commit 1 之后立即跑 fixture NAME_RE 跨人验证（调整 6 / C7）**——但此时 fixture 还没建（fixture 在 commit 2）。**调整流程**：commit 1 内**临时跑一段 inline NAME_RE 验证**（不依赖 fixture）：

```bash
.venv/Scripts/python.exe -c "
import sys; sys.path.insert(0,'scripts')
from check_cross_consistency import NAME_RE
text = '本项目由张三负责整体管理,李四担任技术负责人,王五负责具体实施工作。'
matches = [(m.group(1), m.group(2)) for m in NAME_RE.finditer(text)]
print(f'matches: {matches}')
# 期望: 不含 ('张三', '技术负责人'); 含 ('李四', '技术负责人'); 可能含 ('王五', '实施')
assert ('张三', '技术负责人') not in matches, '跨人误抓!'
print('OK: NAME_RE 不跨人')
"
```

跨人误抓 → commit 1 内收紧 NAME_RE（如改"姓名 + [^姓-名]{0,10}"或"姓名 + (?:[一-龥]{0,10})"等具体策略）。

---

## 6. 自检方式（Phase 3 完工时执行）

```bash
# 1. 单元测试 18 case 全过(V3-2 12 + V3-9 6)
./run_script.bat tests/test_check_cross_consistency.py
# 期望:case 1-18 全 PASS

# 2. demo 烟测(项 4 真实触发,项 5/6 skip)
./run_script.bat check_cross_consistency.py \
    projects/demo_cadre_training/final_tender_package/final_response.docx \
    --brief projects/demo_cadre_training/output/tender_brief.json
# 期望:
#   - [项 4] 项目名不一致警告/失败(demo 天然 dirty)
#   - [项 5] [信息] skip
#   - [项 6] [信息] skip
#   - [项 1/2/3] 行为不变

# 3. fallback 解耦验证:grep 确认 V3-9 三个新函数 0 处使用 tables
.venv/Scripts/python.exe -c "
import re
src = open('scripts/check_cross_consistency.py', encoding='utf-8').read()
for fn in ['check_field_consistency', 'check_resume_org_consistency', 'check_section_ref_validity']:
    m = re.search(rf'def {fn}.*?(?=\ndef |\Z)', src, re.DOTALL)
    if m and 'tables' in m.group(0):
        print(f'FAIL: {fn} 引用 tables 字段')
        break
else:
    print('OK: V3-9 三个函数 0 处使用 tables, V3-6 fallback 解耦确认')
"
```

完工时贴给用户：

1. git diff（2 个 commit）
2. test 18 case PASS 报告
3. demo 烟测输出
4. fallback 解耦 grep 输出
5. C1-C7 逐条对照
6. **D6 文档化声明**：demo 项目名不一致是已知数据问题，V3-9 检测正确触发，不修 demo 数据

---

## 7. 风险

| 风险 | 缓解 |
|---|---|
| 项 4 jaccard 阈值 0.4/0.7 在 demo 烟测可能过严 / 过宽 | demo 烟测后 1 档 autonomous 调整,跨档（< 0.5）停下汇报 |
| 项 5 NAME_RE 在繁体 / 4 字以上少数民族姓名失效 | 范围内接受;V3 不引入 NLP 库;遇真实问题登记 V4 |
| 项 5 NAME_RE 跨人误抓 | commit 1 内立即跑 inline 验证（C7）,误抓则 commit 1 内收紧（不另开 commit）|
| 项 6 章节编号体系非 X.Y.Z 格式（中文混合）| 范围内不支持,fixture/docx_builder 产物均阿拉伯数字;非标场景登记 V4 |
| python-docx Heading style 自定义 / 中文别名 | 检测 'Heading' substring 兼容主流情况,自定义 style 边界登记 V4 |
| demo 烟测项 4 触发 [警告] 而非 [失败] —— demo 用户可能习惯 0 警告通过 | 文档化 D6:demo 数据问题不修,工具检测正确 |
| V3-9 三函数 ~250 行新代码,可能引入 syntax / 缩进错误 | commit 1 内立即跑 import + V3-2 case 1-12 + NAME_RE inline 验证三件套（C5+C7+B/C 都过才 commit 2）|

---

## 8. 待用户审核点（已审）

- **D0**: 项 4 jaccard 阈值 0.7/0.4 + 候选窗口分层（前 500 字符 + Heading 1 + 降级全文 30 字窗口）。✓ 通过（**调整 4** 候选窗口分层已纳入）
- **D1**: 采购方 / 代理代称模式走 [信息] 不报失败。✓ 通过
- **D2**: 项 5 章节关键词清单（**调整 1**: RESUME 加"人员"/ ORG 加"团队组成"，最终各 7+6 个）。✓ 通过
- **D3**: 项 5 NAME_RE 职称白名单（**调整 2**: 加 5 个，最终 15 个：项目经理/项目负责人/技术负责人/主讲/讲师/顾问/专家/总监/副总/经理/架构师/工程师/设计师/实施/运维）。✓ 通过
- **D4**: 项 6 必须含引用动词（详见 / 见 / 参见 / 参考 / 参照）+ 编号。✓ 通过
- **D5**: commit 序列（**调整 3**: 改为 2 个 commit：feat 合并 + test 合并）。✓ 通过
- **D6**: demo 项目名不一致是天然 dirty case，不修 demo，工具触发即正确（**调整 8** Phase 3 review 报告里明确说明）。✓ 通过

**额外约束（落地前需做的）**：

- **调整 5** 表格覆盖：实测确认 `extract_docx_text` L46-56 含 `cell.text` 拼接（**已实测 ✓**），项 6 全文扫描自动覆盖表格 cell；fixture `dirty_section_ref.docx` 加 1 处表格内错引用反例验证
- **调整 6** NAME_RE 跨人验证：commit 1 内立即跑 inline 验证；fixture clean_response.docx 含同段两人名场景；跨人误抓 → commit 1 内收紧（不另开 commit）（C7）
- **调整 7** commit 1 自检加强：除 import 验证外，立即跑 V3-2 case 1-12 零回归（C5）

审核已通过，Phase 2 启动：2 个 commit 按 §5 执行。
