# V3-10 执行计划 · README + SKILL 阶段口径统一

> 本计划是 V3-10 在 Plan-Execute-Review 协议下的 Phase 1 产出。审核通过后进 Phase 2 自主 commit。
>
> V3-10 是 1h 文案小活——`docs/DESIGN.md` 已经改成"五阶段主干 + 并列阶段(4-B/4-C/6)" 口径,但 README / SKILL / .clinerules / install.bat / demo README / run_script.bat 仍单口径"五阶段"。本期统一措辞到 DESIGN.md ground truth。
>
> Plan 阶段已实测全仓 grep 命中 + DESIGN.md 当前口径(对齐 feedback_phase0_paste_command_output 教训:**实测命令输出贴 plan,真跑 wc -l 不是估数**——V3-13 plan §0.2 写"43 个文件"实测 47,本期严格避免)。

---

## 0. Phase 0 关键发现(实测命令输出贴 plan)

### 0.1 grep "五阶段" 全仓命中(20 行)

```text
$ grep -rn "五阶段" .
.clinerules:16:1. 用 `read_file` 读取 `SKILL.md` 理解五阶段工作流
.clinerules:23:"SKILL 已加载,五阶段工作流就绪,请告诉我本次招标文件位置和项目信息。"
.clinerules:27:## 1. 五阶段硬性顺序
docs/v3_planning.md:203:**现状**:docs/DESIGN.md 已经改成"主干 5 + 并列(4-B/4-C/6)",但 README L31 / L49 / L67 + SKILL.md description 字段 / L44 标题 还是单口径"五阶段"。
docs/v3_planning.md:207:- README 三处 + SKILL.md description 字段 + SKILL.md L44 标题改为"五阶段主干 + 并列阶段(4-B/4-C/6)"
docs/v3_planning.md:210:**完成判定**:全仓 grep "五阶段" 命中均与"主干 + 并列"语义匹配
docs/DESIGN.md:7:## 为什么是五阶段主干加独立并列阶段
install.bat:115:    echo     tender-writer\SKILL.md 并按其中的五阶段工作流推进
install.bat:119:    echo     可参考 SKILL.md 文本作为提示词素材,但无法执行五阶段脚本。
projects/demo_cadre_training/README.md:90:# 3. 按 SKILL.md 五阶段流程,在 Claude Code 里:
SKILL.md:3:[Omitted: description 字段含 "...五阶段工作流推进..." 一段]
SKILL.md:16:> 3. 本文档(SKILL.md) — 五阶段工作流程
SKILL.md:44:## 二、五阶段工作流(严格分阶段,绝不允许跳过或一键生成)
SKILL.md:940:   - 五阶段是不可压缩的。任何"帮我一次性把标书写完"的请求,都必须解释...
README.md:31:- **五阶段工作流**:招标文件解析 → 评分矩阵 → 提纲 → 分章节撰写 → 合规终审,每阶段可 review
README.md:50:- **Claude Code(推荐)**:项目级协作深度最优,读取 `SKILL.md` + `CLAUDE.md` 走完整五阶段;...
README.md:68:| [SKILL.md](SKILL.md) | 五阶段工作流细节,AI 入口,必读 |
run_script.bat:6:REM        在五阶段工作流中应统一通过本脚本调用 Python 脚本,
plans/v3-11.md:5:> V3-11 是 V3-1 的对外口径配套:...本计划只动 B 模式措辞,不动 V3-10(五阶段口径)/ V3-12(description 精简)。
plans/v3-11.md:22:- README L31 / L43 / L49 / L67 涉及"五阶段" — 留 V3-10
```

**实测分类(20 行总命中)**:

| 类别 | 行数 | 说明 |
|---|---|---|
| **要改** | 14 | 见 §2 改动表 |
| **不动 — ground truth** | 1 | DESIGN.md L7(本身就是新口径"五阶段主干加独立并列阶段") |
| **不动 — plan 历史快照** | 5 | v3_planning.md×3(L203/L207/L210)+ plans/v3-11.md×2(L5/L22)|

### 0.2 grep 变体 "5 阶段|5个阶段|五个阶段" 全仓命中(1 行)

```text
$ grep -rn -E "5\s*个阶段|5阶段|五个阶段|5\s*阶段" .
plans/v3-3.md:339:**注意**:不是所有脚本都简单跑得起来...至少跑 4-5 个阶段让 _timing.json 见证多 stage 即可...
```

**分类**:`4-5 个阶段` 是数量表述(timing 脚本运行覆盖度),**不是流程定语**,**不改**。

### 0.3 DESIGN.md ground truth 完整原文(作 plan 改动参考)

```markdown
## 为什么是五阶段主干加独立并列阶段

标书编制的本质是"把招标文件的要求,逐项映射成可验证的应答"。v2 沿用
5 个主干阶段(解析→矩阵→提纲→撰写→合规终审),另外并列出**阶段 4-B
/ 4-C**(按生产模式分流的 B 模式材料组装 / C 模式模板填充)和**阶段
6**(交付层导出)——主干先稳,并列按需。
```

**新口径标准措辞**(本期统一):

- 主标题级:`五阶段主干 + 并列阶段(4-B / 4-C / 6)`
- 简式:`五阶段主干 + 并列阶段`
- 全式(只在 README L31 这种核心展示位用):`五阶段主干工作流:...,另含并列阶段 4-B(B 模式材料组装) / 4-C(C 模式模板填充) / 6(交付层导出)`

不同位置选合适长度的措辞,避免每处堆完整一长串。

### 0.4 V3-12 范围保护(必须明确)

`SKILL.md` L3 description 字段含两个不同范围的潜在改动:

1. **V3-10 范围**(本期改):`...严格按照"招标文件解析→评分矩阵构建→提纲生成→分章节撰写→合规终审"五阶段工作流推进...` 这一句中的 `"五阶段"` 措辞 → `"五阶段主干 + 并列阶段(4-B/4-C/6)"`
2. **V3-12 范围**(留 V3-12 做):description 开头 40+ 触发关键词清单(`当用户上传招标文件、招标公告、采购文件,或提到投标、技术标、响应文件、投标方案、技术方案应答、评分办法、废标条款、实质性响应、资格要求、政府采购、竞争性磋商、单一来源采购、技术标书、商务标、投标书、投标响应文件、应答文件、标书编制、标书撰写、采购需求应答、招投标文件解读、评标办法解析等关键词时触发`)精简到 ~20 个

V3-10 **严格只动 (1)** 的"五阶段"措辞,**绝不碰** (2) 的触发关键词清单。`v3_planning.md` L208 写"SKILL.md description 字段顺手砍 40+ 关键词堆砌的一半"是 V3-10 旧口径,本期收紧到只改"五阶段"措辞。

---

## 1. 范围与完成判定

### 1.1 范围

**做**:

- 改 14 处"五阶段" → 新口径(具体见 §2 改动表)
- 沿用 DESIGN.md L7 ground truth 措辞,不发明新表述

**不做**:

- 不动 DESIGN.md(本身已是 ground truth)
- 不动 plan 历史(v3_planning.md / plans/v3-11.md / plans/v3-3.md)
- 不动 SKILL.md description **触发关键词清单**(留 V3-12)
- 不改 SKILL.md / README.md 任何与"五阶段"无关的措辞
- 不写 commit / push 任何脚本逻辑改动(纯文案)

### 1.2 完成判定

| # | 完成判定 | 自检方式 |
|---|---|---|
| C1 | 14 处全部改完,文字与 §2 表完全一致 | 改完跑 grep 看每行新内容 |
| C2 | 改完后 grep "五阶段" 总命中仍为 20 行,但**14 处新含"主干 + 并列"语义** | 跑 grep 对比新旧输出 |
| C3 | DESIGN.md L7 / plan 历史 5 处**不变** | git diff 看这 6 处行号无改动 |
| C4 | SKILL.md description 触发关键词清单**不动** | git diff SKILL.md 看 L3 只改"五阶段"那一段, 不动关键词 |
| C5 | install.bat / run_script.bat 文案改后语法仍正确(无 echo 转义破坏) | install.bat 双击或 run_script.bat 跑 `--help` 看输出正常 |
| C6 | demo 跑端到端不破坏 | `./run_script.bat demo_reset.py` 验证 |

---

## 2. 改动表(14 处,精确到行)

| # | 文件 | 行 | 原文(关键片段) | 拟改(关键片段) |
|---|---|---|---|---|
| 1 | `.clinerules` | 16 | `理解五阶段工作流` | `理解五阶段主干 + 并列阶段(4-B/4-C/6)工作流` |
| 2 | `.clinerules` | 23 | `"SKILL 已加载,五阶段工作流就绪...` | `"SKILL 已加载,五阶段主干 + 并列阶段(4-B/4-C/6)工作流就绪...` |
| 3 | `.clinerules` | 27 | `## 1. 五阶段硬性顺序` | `## 1. 五阶段主干 + 并列阶段硬性顺序` |
| 4 | `install.bat` | 115 | `按其中的五阶段工作流推进` | `按其中的五阶段主干 + 并列阶段(4-B/4-C/6)工作流推进` |
| 5 | `install.bat` | 119 | `但无法执行五阶段脚本` | `但无法执行五阶段主干 + 并列阶段脚本` |
| 6 | `projects/demo_cadre_training/README.md` | 90 | `按 SKILL.md 五阶段流程` | `按 SKILL.md 五阶段主干 + 并列阶段流程` |
| 7 | `SKILL.md` | 3 (description 内) | `..."招标文件解析→评分矩阵构建→提纲生成→分章节撰写→合规终审"五阶段工作流推进...` | `..."招标文件解析→评分矩阵构建→提纲生成→分章节撰写→合规终审"五阶段主干 + 并列阶段(4-B/4-C/6)工作流推进...` |
| 8 | `SKILL.md` | 16 | `> 3. 本文档(SKILL.md) — 五阶段工作流程` | `> 3. 本文档(SKILL.md) — 五阶段主干 + 并列阶段(4-B/4-C/6)工作流程` |
| 9 | `SKILL.md` | 44 | `## 二、五阶段工作流(严格分阶段,绝不允许跳过或一键生成)` | `## 二、五阶段主干 + 并列阶段(4-B/4-C/6)工作流(严格分阶段,绝不允许跳过或一键生成)` |
| 10 | `SKILL.md` | 940 | `- 五阶段是不可压缩的。任何"帮我一次性..."` | `- 五阶段主干 + 并列阶段是不可压缩的。任何"帮我一次性..."` |
| 11 | `README.md` | 31 | `- **五阶段工作流**:招标文件解析 → 评分矩阵 → 提纲 → 分章节撰写 → 合规终审,每阶段可 review` | `- **五阶段主干工作流**:招标文件解析 → 评分矩阵 → 提纲 → 分章节撰写 → 合规终审,每阶段可 review;另含并列阶段 4-B(B 模式材料组装) / 4-C(C 模式模板填充) / 6(交付层导出)` |
| 12 | `README.md` | 50 | `走完整五阶段;` | `走完整五阶段主干 + 并列阶段;` |
| 13 | `README.md` | 68 | `\| [SKILL.md](SKILL.md) \| 五阶段工作流细节,AI 入口,必读 \|` | `\| [SKILL.md](SKILL.md) \| 五阶段主干 + 并列阶段工作流细节,AI 入口,必读 \|` |
| 14 | `run_script.bat` | 6 | `在五阶段工作流中应统一通过本脚本调用 Python 脚本` | `在五阶段主干 + 并列阶段工作流中应统一通过本脚本调用 Python 脚本` |

**措辞长度分布**:
- 5 处用全式 `(4-B/4-C/6)` 标注:.clinerules×2(L16/L23) + install.bat L115 + SKILL.md L7/L16/L44(description/L16/L44 标题级)
- 9 处用简式 `主干 + 并列阶段`:其余位置(避免每处堆完整一长串)
- 1 处用扩展式(README L31 核心展示位含"4-B(B 模式材料组装)/ 4-C(C 模式模板填充)/ 6(交付层导出)"完整解释)

---

## 3. 实施顺序与回滚策略

### 3.1 实施顺序(降低破坏风险)

1. 先改 markdown 类(README.md / SKILL.md / .clinerules / demo README)——**纯文本无语法风险**
2. 后改 .bat 类(install.bat / run_script.bat)——**echo 行 + 中文 + 英文符号要小心**

### 3.2 .bat 文件改动注意

`install.bat` L115/L119 在 `echo` 命令内,echo 中文段已用 chcp 65001 处理。改动只是字符串内文本,不影响 echo 语法。但要注意:
- 不引入英文括号 `(` `)` 在 echo 内未转义(原文有 `tender-writer\SKILL.md` 这种 path,新口径"4-B/4-C/6"中的 `(` `)` 需要 `^(` `^)` 转义)
- **改动用全式时不进 .bat 内**(避免转义复杂),只用简式 `主干 + 并列阶段`

修订 install.bat L115 为 `按其中的五阶段主干 + 并列阶段工作流推进`(简式,避免 .bat 转义);run_script.bat L6 同理(已是简式)。

### 3.3 回滚

V3-10 是纯文案改,任何回滚 `git restore <file>` 就够,无中间状态污染风险。

---

## 4. 测试 fixture / 自检设计

V3-10 文案改**不需要单元测试**(无新代码 / 函数 / 行为)。Phase 3 自检靠 grep 对比 + demo 烟测。

### 4.1 改完 grep 对比(C1 + C2 完成判定)

```bash
# 1. 改完后命中行数应仍是 20
grep -rn "五阶段" . --include='*.md' --include='*.bat' --include='.clinerules' | wc -l
# 期望: 20

# 2. "裸"五阶段(不带"主干"的)应只剩 ground truth + plan 历史 = 6 行
grep -rn "五阶段" . --include='*.md' --include='*.bat' --include='.clinerules' \
    | grep -v "五阶段主干" \
    | grep -v "DESIGN.md\|v3_planning.md\|v3-11.md\|v3-3.md"
# 期望: 0 行命中(所有应改的都改完了)

# 3. 反向: 含"五阶段主干"的命中应 ≥ 14
grep -rn "五阶段主干" . | wc -l
# 期望: ≥ 14
```

### 4.2 demo 端到端不破坏(C5 + C6)

```bash
# install.bat 不实跑(避免重装),只检查文件 echo 字符串语法
type install.bat | head -125
# 人眼快速看 L115/L119 echo 是否完整(无未配对 ( 和 ))

# run_script.bat 跑 --help
./run_script.bat parse_tender.py --help
# 期望: 帮助文本正常输出,run_script.bat 注释改动不影响执行

# demo_reset --dry-run
./run_script.bat demo_reset.py --dry-run
# 期望: 正常输出,demo 状态不变
```

---

## 5. Commit 序列(Phase 2 自主执行,1 个 commit)

| # | message | 动到的文件 |
|---|---|---|
| 1 | `docs(v3-10): unify "五阶段" wording to "五阶段主干 + 并列阶段(4-B/4-C/6)"` | `.clinerules` + `install.bat` + `projects/demo_cadre_training/README.md` + `SKILL.md` + `README.md` + `run_script.bat` (共 6 个文件,14 处改动) |

V3-10 是纯文案 1h 短任务,**1 个 commit 全部落地**。收尾 commit `docs(v3-10): mark completed` 由用户审过 Phase 3 后再做。

---

## 6. 自检方式(Phase 3 完工时执行)

```bash
# 1. grep 对比验证 C1 / C2 / C3 / C4
grep -rn "五阶段" . --include='*.md' --include='*.bat' --include='.clinerules' | wc -l
# 期望: 20

grep -rn "五阶段主干" . | wc -l
# 期望: ≥ 14

# 2. SKILL.md description 触发关键词清单未动 (V3-12 范围保护 C4)
.venv/Scripts/python.exe -c "
text = open('SKILL.md', encoding='utf-8').read()
# description 开头那段触发关键词
keywords_segment = '当用户上传招标文件、招标公告、采购文件'
print('触发关键词段不变:', keywords_segment in text)
"
# 期望: True

# 3. demo 端到端不破坏 (C5 / C6)
./run_script.bat demo_reset.py --dry-run
./run_script.bat parse_tender.py --help

# 4. install.bat 看 L115/119 echo 完整(肉眼)
sed -n '110,125p' install.bat
```

完工时贴给用户:

1. git diff (1 个 commit)
2. grep 三段对比输出
3. demo 烟测输出
4. C1-C6 逐条对照

---

## 7. 风险

| 风险 | 缓解 |
|---|---|
| .bat 文件 echo 中插入英文括号 `(4-B/4-C/6)` 未转义,执行 install.bat 报语法错 | install.bat / run_script.bat 用**简式** `主干 + 并列阶段`(不带括号),只 markdown 用全式 `(4-B/4-C/6)` |
| SKILL.md description 长度变化触发 Claude Skill 加载行为变化 | description 字段只改"五阶段"那一句,长度变化 < 30 字符,加载行为不变 |
| V3-12 后续做 description 精简时,V3-10 改的"五阶段主干 + 并列阶段"措辞被 V3-12 误删 | V3-12 plan 写时显式注明:精简关键词清单,**不动**"五阶段主干 + 并列阶段"复合词措辞 |
| README L31 全式扩展到含"4-B(B 模式材料组装)/ 4-C(C 模式模板填充)/ 6(交付层导出)" 后行变长,markdown 列表换行美感受影响 | 接受;核心展示位需要完整解释,不为了行长度牺牲信息密度 |
| .clinerules 加全式后行变长,Cline 加载格式美感受影响 | 接受;Cline 是开发期辅助工具,不在 V3-12 精简范围内 |

---

## 8. 待用户审核点

请用户审核以下决策:

- **D0**: 14 处改动表(§2),措辞长度分布(5 处全式 + 9 处简式 + 1 处扩展式)。OK?
- **D1**: install.bat / run_script.bat 用**简式** `主干 + 并列阶段`(不带括号),避免 .bat 转义复杂。OK?
- **D2**: README.md L31 用扩展式含"4-B(B 模式材料组装)/ 4-C(C 模式模板填充)/ 6(交付层导出)"完整解释,接受行变长。OK?
- **D3**: SKILL.md description 字段**只改"五阶段"那一句**,严格不动 40+ 触发关键词清单(留 V3-12)。OK?
- **D4**: DESIGN.md L7 + plan 历史 5 处不动(已是 ground truth / 历史快照)。OK?
- **D5**: 1 个 commit 完成全部 14 处改动,不拆细。OK?
- **D6**: V3-10 不写单元测试(纯文案,无新代码 / 函数 / 行为)。OK?

审核通过后,我做 Phase 1 闭环 commit + Phase 2 自主 1 个 commit + 完工 Phase 3 审核。
