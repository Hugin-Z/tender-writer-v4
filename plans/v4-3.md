# plans/v4-3.md — V4-3 C-reference 诚实化收尾

## 任务定义

V4-3 经两轮 Phase 0 实测收敛为「诚实化收尾」:C-reference 决策层(brief 解析 / merge 分流 / export 映射)已在 fixture 层验证,执行端样例(真实 instructions.md 落盘)推迟到 V4 整体完成后由真实使用场景驱动。本任务不建样例、不动业务代码,只消除「声称 V4-3 会补」产生的 R10 失真 + 登记债务。

## scope 边界(钉死)

动:
- scripts/build_baseline.py(1 处注释末句)
- scripts/tests/test_export_deliverables.py(加 1 行注释)
- scripts/tests/test_v45_merge.py(加 1 行注释)
- docs/changelog.md(加 1 个 V4-3 entry)

不动(越界即停下报告):
- 任何业务代码逻辑(c_mode_extract / c_mode_fill / v45_merge / parse_tender / export_deliverables / brief_schema 源码)
- 任何 fixture JSON / .docx / .json 产物
- test 的 print / 描述字面本身、断言本身
- docs/business_model_v1.md / SKILL.md
- handbook 仓(整个不碰)
- changelog 既有 entry 字面

完成判定:4 条(见自检方式)。

## 显式不做项(防未来误判为遗漏)

- fixture JSON「声明 C-reference Part 但执行端无样例」的 silent 维度:不处理,由 S3 changelog 执行端债务条目覆盖
- handbook projects/tender-writer/ISC.md:不动,债留项目仓 changelog,handbook 等 V4 完成一次性 distill

## 子任务 + commit 序列

### S1 — build_baseline.py 注释诚实化

commit 1: `V4-3.1 build_baseline 注释诚实化:C-reference 首例推迟 V4 完成后真实场景驱动`

改 scripts/build_baseline.py 中 C-reference 注释的末句。
- 落盘前先实测当前确切字面(行号以实测为准,用字面 anchor 不用行号偏移)
- 旧字面 anchor(需消除):`C-reference 首例待 V4-3 收尾时由 demo 真实跑通后补 entry。`
- 新字面:`C-reference 首例执行端样例 V4-3 评估后决定不建(demo_cadre_training 无单独提交件需求),推迟到 V4 整体完成后由真实使用场景驱动补 entry。`
- 注释前半段(V4-0.2 修正 / audit §5 #2 / sub_mode=C-template 实测)保留不动,只替换末句

### S2 — 两处测试 case 加防误读注释

commit 2: `V4-3.2 C-reference 测试 case 加防误读注释:函数级断言非端到端`

不动任何 print / 描述字面本身,只在对应 case 上方加注释行。
- test_export_deliverables.py:在 case_2_a_b_c_mixed 的 print 行上方加:
  `# 注:本 case 验 build_deliverable_mapping() 返回的路径映射字符串,不验 instructions.md 产物实际落盘(C-reference 执行端样例待 V4 完成后由真实场景驱动)`
- test_v45_merge.py:在 case_2_a_b_c_mixed fixture 描述上方加:
  `# 注:验 build_merge_order() 纯函数分流,不验执行端渲染落盘`
- 缩进对齐周边代码;落盘用字面 anchor 定位 case(case_2_a_b_c_mixed),不用行号

### S3 — changelog 登记 V4-3 entry

commit 3: `V4-3.3 changelog 登记 V4-3 收尾 + C-reference 执行端样例推迟`

docs/changelog.md 顶部加 V4-3 entry(既有 entry 字面零改动)。entry 完整字面由 chat 端起草后给(Phase 2 落此 commit 前停下,等 chat 端 entry 字面)。核心三点:
- V4-3 范围 = C-reference 诚实化收尾(非实现)
- C-reference 执行端样例推迟到 V4 整体完成后由真实使用场景驱动
- 此债 + _workspace/audit-2026-05-27.md 共同作为 V4 完成时 handbook distill 的输入

## 自检方式(Phase 3 完成判定逐条对照)

1. S1 改对:`grep -n "V4-3 收尾时由 demo" scripts/build_baseline.py` → 0 命中;`grep -n "推迟到 V4 整体完成后" scripts/build_baseline.py` → 1 命中
2. S2 注释在且未动 print:两测试文件 git diff 只有 + 注释行,无 - 行涉及 print / 描述字面
3. changelog entry 在且历史未动:git diff docs/changelog.md 只有新增 V4-3 entry,既有 entry 0 改动
4. business 代码零改动 + 全测试绿:git diff 不含任何业务源码 / fixture / 产物;`.venv/Scripts/python.exe scripts/tests/run_all.py` → 13/13 PASS

## Phase 2 执行注意

- 落盘一律字面 anchor 不用行号偏移(加注释会让后续行号漂)
- 每步落盘前实测当前确切字面,plan 标的字面若与实测不符以实测为准、surface 留痕
- commit 3 落盘前停下,等 chat 端给 changelog entry 字面
- 三个 commit 线性落,Phase 2 不贴中间审,完工后供 Phase 3 审三件(git log + 整体 diff + 自检报告)